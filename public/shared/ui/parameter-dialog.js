(function () {
    "use strict";
    var tools = window.SharedScriptTools;
    if (!tools) return;
    var sharedOptionProvider = null;
    function text(value) { return String(value == null ? "" : value); }
    function bool(value) { return value === true || /^(1|true|yes|tak|on)$/i.test(text(value)); }
    function variables(item) { return item && Array.isArray(item.variables) ? item.variables : []; }
    function protocolItem(item) { return !!(item && Array.isArray(item.extraHeaders) && item.extraHeaders.some(function (header) { return /^SirkWorkflow\s*:\s*JiraAssetProtocol$/i.test(text(header).trim()); })); }
    function allowCustom(item, variable) { if (variable && variable.allowCustom === true) return true; var name = text(variable && variable.name).trim(); if (!name) return false; return !!(item && Array.isArray(item.extraHeaders) && item.extraHeaders.some(function (header) { var match = /^SirkAllowCustom\s*:\s*(.+)$/i.exec(text(header).trim()); if (!match) return false; return match[1].split(/[;,|]+/).map(function (part) { return part.trim(); }).indexOf(name) >= 0; })); }
    function controlKind(variable) { var kind = text(variable && variable.control).toLowerCase(); return ["select", "switch", "user", "asset", "assetmulti"].indexOf(kind) >= 0 ? kind : "text"; }
    function optionValue(option) { return text(option && typeof option === "object" ? option.value : option); }
    function optionLabel(option) { return text(option && typeof option === "object" ? option.label : option); }
    function writeButtonText(button, value) { if (!button) return; var node = button.querySelector && button.querySelector("span"); if (node) node.textContent = value; else button.textContent = value; }
    function readButtonText(button) { if (!button) return ""; var node = button.querySelector && button.querySelector("span"); return node ? node.textContent : button.textContent; }
    function setStatus(status, message, error) { if (!status) return; status.textContent = message || ""; status.className = "mc-parameter-dialog-status" + (error ? " mc-shared-error" : " mc-shared-muted"); }
    function hostDialogManager() { if (typeof window.setModalContent === "function" && typeof window.showModal === "function") return { mode: "modern", setContent: function (id, title, body) { window.setModalContent(id, title, [], body); }, show: function (id, okButtonId, callback) { window.showModal(id, okButtonId, callback); } }; if (typeof window.setDialogMode === "function") return { mode: "classic", show: function (mode, title, buttons, callback, body) { window.setDialogMode(mode, title, buttons, callback, body); } }; return null; }
    function setOptions(control, options, variable, placeholder) { if (!control || control.tagName !== "SELECT") return; var previous = control.value; control.innerHTML = ""; if (placeholder) { var empty = document.createElement("option"); empty.value = ""; empty.textContent = placeholder; control.appendChild(empty); } (Array.isArray(options) ? options : []).forEach(function (option) { var node = document.createElement("option"); node.value = optionValue(option); node.textContent = optionLabel(option) || node.value; control.appendChild(node); }); var preferred = previous || text(variable && variable.defaultValue); if (preferred) control.value = preferred; }
    function protocolActionOptions(item, variable) { if (!(protocolItem(item) && variable && variable.control === "assetmulti" && text(variable.name) === "PcName")) return []; return [{ value: "none", label: "Bez zmian" }, { value: "receive", label: "Przyjęcie sprzętu" }, { value: "return", label: "Zdanie sprzętu" }]; }
    function setChecklistOptions(host, control, options, single, variable, item) { if (!host || !control) return; var selected = text(control.value).split(/[;,|]+/).filter(Boolean), actions = protocolActionOptions(item, variable); host.innerHTML = ""; (Array.isArray(options) ? options : []).forEach(function (option, index) { var value = optionValue(option), hasActions = actions.length > 0 && !single; var row = document.createElement(hasActions ? "div" : "label"); row.className = "mc-parameter-checklist-item" + (hasActions ? " mc-parameter-checklist-item-actions" : ""); var choice = hasActions ? document.createElement("label") : row; if (hasActions) choice.className = "mc-parameter-checklist-choice"; var box = document.createElement("input"); box.type = single ? "radio" : "checkbox"; box.name = control.name + "Choice"; box.value = value; box.id = control.name + "Choice" + index; box.checked = selected.indexOf(value) >= 0; var label = document.createElement("span"); label.textContent = optionLabel(option) || value; choice.appendChild(box); choice.appendChild(label); if (hasActions) row.appendChild(choice); if (hasActions) { var actionHost = document.createElement("span"); actionHost.className = "mc-parameter-checklist-action"; var actionLabel = document.createElement("span"); actionLabel.className = "mc-parameter-checklist-action-label"; actionLabel.textContent = "Operacja"; var select = document.createElement("select"); select.className = "mc-parameter-checklist-action-select"; select.setAttribute("data-option-value", value); select.setAttribute("aria-label", "Operacja - " + (optionLabel(option) || value)); var disabled = option && typeof option === "object" && Array.isArray(option.disabledActions) ? option.disabledActions.map(text) : []; actions.forEach(function (action) { var node = document.createElement("option"); node.value = action.value; node.textContent = action.label; if (disabled.indexOf(node.value) >= 0) node.disabled = true; select.appendChild(node); }); select.value = "none"; actionHost.appendChild(actionLabel); actionHost.appendChild(select); row.appendChild(actionHost); if (window.MeshThemeAdapter && typeof window.MeshThemeAdapter.control === "function") window.MeshThemeAdapter.control(select); } host.appendChild(row); }); }
    function currentValues(records) { var values = {}; records.forEach(function (record) { if (!record || !record.variable) return; values[record.variable.name] = record.kind === "switch" ? !!record.control.checked : record.control.value; if (record.optionHost && protocolActionOptions(record.item, record.variable).length) { var map = {}; Array.prototype.forEach.call(record.optionHost.querySelectorAll(".mc-parameter-checklist-item-actions"), function (row) { var box = row.querySelector('input[type="checkbox"]'), select = row.querySelector("select[data-option-value]"); if (!box || !select || !box.checked) return; map[text(select.getAttribute("data-option-value") || box.value)] = text(select.value || "none"); }); values.JiraAssetActionsJson = JSON.stringify(map); } }); return values; }
    function validate(records, status) { var values = {}; for (var index = 0; index < records.length; index++) { var record = records[index], variable = record.variable, value = record.kind === "switch" ? !!record.control.checked : record.control.value; if (variable.required && !text(value).trim()) { setStatus(status, (variable.label || variable.name) + " is required.", true); if (record.control && typeof record.control.focus === "function") record.control.focus(); return null; } values[variable.name] = value; } var expanded = currentValues(records); Object.keys(expanded).forEach(function (name) { values[name] = expanded[name]; }); setStatus(status, "", false); return values; }
    function buildContent(item) { var content = document.createElement("div"); content.className = "mc-parameter-dialog-content mc-shared-settings-form"; var description = text(item && item.description).trim(); if (description) { var help = document.createElement("div"); help.className = "mc-shared-muted mc-parameter-dialog-description"; help.textContent = description; content.appendChild(help); } var records = []; variables(item).forEach(function (variable, index) { if (protocolItem(item) && text(variable && variable.name) === "IsTransferProtocol") return; var kind = controlKind(variable), wrapper = document.createElement("label"); wrapper.className = "mc-parameter-dialog-field mc-parameter-dialog-field-" + kind; var inlineControl = kind === "switch" && variable.inlineControl === true; if (inlineControl) wrapper.classList.add("mc-parameter-dialog-field-inline-control"); var title = document.createElement("span"); title.className = "mc-parameter-dialog-label"; title.textContent = text(variable.label || variable.name) + (variable.required ? " *" : ""); var customUser = kind === "user" && allowCustom(item, variable), listMode = variable.listMode === true && (kind === "user" || kind === "select"), multiline = kind === "text" && variable.multiline === true, useSelect = kind === "select" || kind === "asset" || (kind === "user" && !customUser); var control = document.createElement(multiline ? "textarea" : listMode ? "input" : useSelect ? "select" : "input"); control.name = text(variable.name || ("value" + index)); control.className = "mc-definition-input mc-parameter-dialog-input"; if (multiline) { control.rows = Math.max(2, Math.min(20, Number(variable.rows) || 8)); control.wrap = "soft"; } var optionHost = null; if (kind === "switch") { control.type = "checkbox"; control.checked = bool(variable.defaultValue); control.className += " mc-parameter-dialog-switch"; } else if (kind === "assetmulti" || listMode) { control.type = "hidden"; control.value = text(variable.defaultValue); optionHost = document.createElement("div"); optionHost.className = "mc-parameter-checklist"; optionHost.tabIndex = 0; var staticOptions = (kind === "assetmulti" && Array.isArray(variable.options)) || listMode ? variable.options : null; if (Array.isArray(staticOptions)) setChecklistOptions(optionHost, control, staticOptions, listMode, variable, item); } else if (useSelect) setOptions(control, variable.options, variable, variable.required ? "Select…" : "None"); else { control.type = "text"; control.value = text(variable.defaultValue); if (customUser) { var listId = "mc-parameter-options-" + Date.now() + "-" + index; control.setAttribute("list", listId); var dataList = document.createElement("datalist"); dataList.id = listId; wrapper.appendChild(dataList); } } if (variable.inlineLabel === true) wrapper.classList.add("mc-parameter-dialog-field-inline-label"); if (variable.hideLabel !== true) { if (inlineControl) wrapper.appendChild(control); wrapper.appendChild(title); } if (variable.hideLabel === true || !inlineControl) wrapper.appendChild(control); if (optionHost) wrapper.appendChild(optionHost); if (variable.description) { var note = document.createElement("span"); note.className = "mc-shared-muted mc-parameter-dialog-note"; note.textContent = text(variable.description); wrapper.appendChild(note); } content.appendChild(wrapper); records.push({ variable: variable, kind: kind, control: control, optionHost: optionHost, customUser: customUser, allOptions: Array.isArray(variable.options) ? variable.options.slice() : [], listMode: listMode, item: item }); }); var status = document.createElement("div"); status.className = "mc-parameter-dialog-status mc-shared-muted"; content.appendChild(status); return { element: content, records: records, status: status }; }
    function assetUserDependency(records, assetRecord) { if (!assetRecord || (assetRecord.kind !== "asset" && assetRecord.kind !== "assetmulti")) return null; var explicitName = text(assetRecord.variable && assetRecord.variable.jiraAsset && assetRecord.variable.jiraAsset.userVariable).trim(); if (explicitName) return records.filter(function (record) { return text(record.variable && record.variable.name) === explicitName; })[0] || null; return records.filter(function (record) { return record.kind === "user"; })[0] || null; }
    function assetDependsOnUser(records, assetRecord, userRecord) { return !!userRecord && assetUserDependency(records, assetRecord) === userRecord; }
    function firstFocusable(records) { for (var index = 0; index < records.length; index++) { var record = records[index]; if (record.optionHost && record.optionHost.querySelector('input:not([disabled])')) return record.optionHost.querySelector('input:not([disabled])'); if (record.control && record.control.type !== "hidden" && !record.control.disabled) return record.control; } return null; }
    function openParameterDialog(options) { options = options || {}; var item = options.item || {}, manager = hostDialogManager(); if (!manager) return Promise.reject(new Error("Native MeshCentral dialog manager is unavailable.")); var built = buildContent(item), title = text(options.title || item.label || item.name || "Parameters"); if (manager.mode === "modern") manager.setContent("xxAddAgent", title, built.element.innerHTML); else manager.show(2, title, 3, null, built.element.innerHTML); var status = document.querySelector(".mc-parameter-dialog-status"); var records = built.records.map(function (record) { var control = document.getElementsByName(record.control.name)[0], wrapper = control && control.closest ? control.closest(".mc-parameter-dialog-field") : null, optionHost = wrapper && wrapper.querySelector ? wrapper.querySelector(".mc-parameter-checklist") : null, dataList = wrapper && wrapper.querySelector ? wrapper.querySelector("datalist") : null; return Object.assign({}, record, { control: control, optionHost: optionHost, dataList: dataList }); }).filter(function (record) { return !!record.control; }); records.forEach(function (record) { if (window.MeshThemeAdapter && typeof window.MeshThemeAdapter.control === "function") window.MeshThemeAdapter.control(record.control); }); var submit = document.getElementById("idx_dlgOkButton"), cancel = document.getElementById("idx_dlgCancelButton"), close = document.getElementById("id_dialogclose"); if (!submit) return Promise.reject(new Error("Native MeshCentral dialog controls are unavailable.")); var trigger = options.trigger || document.activeElement, primaryLabel = text(options.primaryLabel || "Run"), originalSubmitText = readButtonText(submit), originalSubmitDisabled = !!submit.disabled, modernModal = document.getElementById("xxAddAgentModal"), settled = false, submitting = false, submittedValues = null, cleaned = false, loadSequence = 0, valuesChangeSequence = 0, valuesChangePending = false, valuesChangeFailed = false; var provider = options.resolveOptions === null ? null : (typeof options.resolveOptions === "function" ? options.resolveOptions : sharedOptionProvider); return new Promise(function (resolve) { function restoreFocus() { if (trigger && trigger.isConnected !== false && typeof trigger.focus === "function") trigger.focus(); } function cleanup() { if (cleaned) return; cleaned = true; loadSequence++; valuesChangeSequence++; valuesChangePending = false; valuesChangeFailed = false; records.forEach(function (record) { if (record.kind === "user") record.control.removeEventListener("change", onUserChanged); if (record.optionHost) { record.optionHost.removeEventListener("change", onChecklistChanged); if (record.variable.submitOnDoubleClick === true) record.optionHost.removeEventListener("dblclick", onOptionDoubleClick); } if (record.kind === "text") record.control.removeEventListener("input", onFilterChanged); else if (record.kind === "switch") record.control.removeEventListener("change", onFilterChanged); }); if (manager.mode === "classic") submit.removeEventListener("click", onClassicSubmit, true); if (cancel) cancel.removeEventListener("click", onCancel, true); if (close) close.removeEventListener("click", onCancel, true); if (modernModal) modernModal.removeEventListener("hidden.bs.modal", onHidden); writeButtonText(submit, originalSubmitText); submit.disabled = originalSubmitDisabled; restoreFocus(); } function finish(value) { if (settled) return; settled = true; resolve(value); } function onCancel() { finish(null); cleanup(); } function onHidden() { if (!settled) finish(submitting ? submittedValues : null); cleanup(); } function dynamicRecord(record) { return record.kind === "user" || record.kind === "asset" || record.kind === "assetmulti"; } function setRecordDisabled(record, disabled) { if (record.control) record.control.disabled = disabled; if (record.optionHost) Array.prototype.forEach.call(record.optionHost.querySelectorAll("input,select"), function (control) { control.disabled = disabled; }); } function refreshSubmitState() { if (!submit) return; var loading = records.some(function (record) { return !!record.loading; }); submit.disabled = originalSubmitDisabled || submitting || loading || valuesChangePending || valuesChangeFailed; } function loadDynamic(record) { if (!record || !dynamicRecord(record) || typeof provider !== "function") return Promise.resolve(); if ((record.kind === "asset" || record.kind === "assetmulti") && assetUserDependency(records, record)) { var dependency = assetUserDependency(records, record); if (!dependency || !text(dependency.control.value).trim()) { if (record.optionHost) setChecklistOptions(record.optionHost, record.control, [], false, record.variable, record.item); else if (record.control.tagName === "SELECT") setOptions(record.control, [], record.variable, record.variable.required ? "Select…" : "None"); setRecordDisabled(record, true); return Promise.resolve(); } } var sequence = ++loadSequence; record.loading = true; setRecordDisabled(record, true); setStatus(status, "Loading " + text(record.variable.label || record.variable.name) + "…", false); refreshSubmitState(); var result; try { result = provider(record.variable, currentValues(records), item); } catch (error) { result = Promise.reject(error); } return Promise.resolve(result).then(function (optionsValue) { if (cleaned || sequence !== loadSequence) return; record.allOptions = Array.isArray(optionsValue) ? optionsValue.slice() : []; if (record.kind === "user" && record.customUser) { if (record.dataList) { record.dataList.innerHTML = ""; record.allOptions.forEach(function (option) { var node = document.createElement("option"); node.value = optionValue(option); node.textContent = optionLabel(option) || node.value; record.dataList.appendChild(node); }); } } else if (record.optionHost) { setChecklistOptions(record.optionHost, record.control, record.allOptions, record.listMode, record.variable, record.item); if (record.kind === "user") applyUserFilter(record); } else setOptions(record.control, record.allOptions, record.variable, record.variable.required ? "Select…" : "None"); record.loading = false; setRecordDisabled(record, false); setStatus(status, "", false); refreshSubmitState(); }).catch(function (error) { if (cleaned || sequence !== loadSequence) return; record.loading = false; setRecordDisabled(record, false); setStatus(status, error && error.message || String(error), true); refreshSubmitState(); }); } function notifyValuesChanged(record) { if (typeof options.onValuesChanged !== "function" || !record) return null; var sequence = ++valuesChangeSequence, operation; try { operation = options.onValuesChanged(currentValues(records), record.variable, item); } catch (error) { operation = Promise.reject(error); } if (!operation || typeof operation.then !== "function") return null; valuesChangePending = true; valuesChangeFailed = false; setStatus(status, text(options.valuesChangePendingMessage || "Loading..."), false); refreshSubmitState(); return Promise.resolve(operation).then(function (value) { if (cleaned || sequence !== valuesChangeSequence) return value; valuesChangePending = false; valuesChangeFailed = false; setStatus(status, "", false); refreshSubmitState(); return value; }).catch(function (error) { if (cleaned || sequence !== valuesChangeSequence) return; valuesChangePending = false; valuesChangeFailed = true; setStatus(status, error && error.message || String(error), true); refreshSubmitState(); }); } function valueRecord(name) { name = text(name); return records.filter(function (record) { return text(record.variable && record.variable.name) === name; })[0] || null; } function applyUserFilter(record) { if (!record || record.kind !== "user" || record.customUser || (!record.variable.searchVariable && !record.variable.activeOnlyVariable)) { if (record && record.kind !== "assetmulti" && !record.listMode) setOptions(record.control, record.allOptions, record.variable, record.variable.required ? "Select…" : "None"); return; } var searchRecord = valueRecord(record.variable.searchVariable), activeRecord = valueRecord(record.variable.activeOnlyVariable), query = text(searchRecord && searchRecord.control.value).trim().toLowerCase(), activeOnly = !activeRecord || activeRecord.control.checked, filtered = record.allOptions.filter(function (option) { if (activeOnly && option && typeof option === "object" && option.active === false) return false; return !query || (optionLabel(option) + " " + optionValue(option)).toLowerCase().indexOf(query) >= 0; }), previous = record.control.value; if (record.listMode) setChecklistOptions(record.optionHost, record.control, filtered, true, record.variable, record.item); else setOptions(record.control, filtered, record.variable, record.variable.required ? "Select…" : "None"); if (previous && filtered.some(function (option) { return optionValue(option) === previous; })) record.control.value = previous; } function onFilterChanged(event) { var changedName = text(event && event.currentTarget && event.currentTarget.name); records.filter(function (record) { return record.kind === "user" && (text(record.variable.searchVariable) === changedName || text(record.variable.activeOnlyVariable) === changedName); }).forEach(applyUserFilter); } function onChecklistChanged(event) { var host = event && event.currentTarget, record = records.filter(function (candidate) { return candidate.optionHost === host; })[0]; if (!record) return; record.control.value = Array.prototype.filter.call(host.querySelectorAll('input[type="checkbox"],input[type="radio"]'), function (box) { return box.checked; }).map(function (box) { return box.value; }).join(";"); setStatus(status, "", false); notifyValuesChanged(record); } function onOptionDoubleClick(event) { var host = event && event.currentTarget, record = records.filter(function (candidate) { return candidate.optionHost === host; })[0], option = event && event.target && event.target.closest ? event.target.closest("label") : null, box = option && option.querySelector ? option.querySelector('input[type="radio"]') : null; if (!record || !box || record.variable.submitOnDoubleClick !== true) return; box.checked = true; record.control.value = box.value; var pending = notifyValuesChanged(record); if (pending) pending.then(function () { if (!cleaned && !valuesChangeFailed) submitRequest(); }); else submitRequest(); } function onUserChanged(event) { if (!event || !event.currentTarget) return; var userRecord = records.filter(function (record) { return record.kind === "user" && record.control === event.currentTarget; })[0] || null; notifyValuesChanged(userRecord); records.filter(function (record) { return (record.kind === "asset" || record.kind === "assetmulti") && assetDependsOnUser(records, record, userRecord); }).forEach(loadDynamic); } function submitRequest() { if (submitting || settled) return false; var values = validate(records, status); if (!values) return false; submitting = true; submittedValues = values; refreshSubmitState(); if (manager.mode === "classic") { finish(values); cleanup(); } return true; } function onClassicSubmit(event) { if (submitRequest()) return; if (event) { if (event.preventDefault) event.preventDefault(); if (event.stopImmediatePropagation) event.stopImmediatePropagation(); else if (event.stopPropagation) event.stopPropagation(); } } writeButtonText(submit, primaryLabel); refreshSubmitState(); records.filter(function (record) { return record.kind === "user"; }).forEach(function (record) { record.control.addEventListener("change", onUserChanged); }); records.forEach(function (record) { if (record.optionHost) { record.optionHost.addEventListener("change", onChecklistChanged); if (record.variable.submitOnDoubleClick === true) record.optionHost.addEventListener("dblclick", onOptionDoubleClick); } if (record.kind === "text") record.control.addEventListener("input", onFilterChanged); else if (record.kind === "switch") record.control.addEventListener("change", onFilterChanged); }); if (cancel) cancel.addEventListener("click", onCancel, true); if (close) close.addEventListener("click", onCancel, true); if (manager.mode === "modern") { manager.show("xxAddAgentModal", "idx_dlgOkButton", submitRequest); if (modernModal) modernModal.addEventListener("hidden.bs.modal", onHidden); refreshSubmitState(); } else submit.addEventListener("click", onClassicSubmit, true); Promise.all(records.filter(function (record) { return record.kind === "user" || record.kind === "asset" || record.kind === "assetmulti"; }).map(loadDynamic)).then(function () { if (cleaned) return; var focus = firstFocusable(records); if (focus && typeof focus.focus === "function") focus.focus(); }).catch(function (error) { if (!cleaned) { setStatus(status, error && error.message || String(error), true); refreshSubmitState(); } }); }); }
    function openConfirmationDialog(options) { options = options || {}; var manager = hostDialogManager(); if (!manager) return Promise.reject(new Error("Native MeshCentral dialog manager is unavailable.")); var content = document.createElement("div"); content.className = "mc-parameter-dialog-content"; var message = document.createElement("p"); message.className = "mc-shared-muted"; message.textContent = text(options.message || "Confirm execution."); content.appendChild(message); var title = text(options.title || "Confirm execution"); if (manager.mode === "modern") manager.setContent("xxAddAgent", title, content.innerHTML); else manager.show(2, title, 3, null, content.innerHTML); var submit = document.getElementById("idx_dlgOkButton"), cancel = document.getElementById("idx_dlgCancelButton"), close = document.getElementById("id_dialogclose"); if (!submit) return Promise.reject(new Error("Native MeshCentral confirmation controls are unavailable.")); var trigger = options.trigger || document.activeElement, primaryLabel = text(options.primaryLabel || "Run"), originalSubmitText = readButtonText(submit), originalSubmitDisabled = !!submit.disabled, modernModal = document.getElementById("xxAddAgentModal"), settled = false, cleaned = false; return new Promise(function (resolve) { function restoreFocus() { if (trigger && trigger.isConnected !== false && typeof trigger.focus === "function") trigger.focus(); } function cleanup() { if (cleaned) return; cleaned = true; if (manager.mode === "classic") submit.removeEventListener("click", onAccept, true); if (cancel) cancel.removeEventListener("click", onCancel, true); if (close) close.removeEventListener("click", onCancel, true); if (modernModal) modernModal.removeEventListener("hidden.bs.modal", onHidden); writeButtonText(submit, originalSubmitText); submit.disabled = originalSubmitDisabled; restoreFocus(); } function finish(value) { if (settled) return; settled = true; resolve(value); } function onAccept(event) { if (event && manager.mode === "classic") { if (event.preventDefault) event.preventDefault(); if (event.stopImmediatePropagation) event.stopImmediatePropagation(); else if (event.stopPropagation) event.stopPropagation(); } finish(true); if (manager.mode === "classic") cleanup(); return true; } function onCancel() { finish(false); cleanup(); } function onHidden() { if (!settled) finish(false); cleanup(); } writeButtonText(submit, primaryLabel); submit.disabled = false; if (cancel) cancel.addEventListener("click", onCancel, true); if (close) close.addEventListener("click", onCancel, true); if (manager.mode === "modern") { if (modernModal) modernModal.addEventListener("hidden.bs.modal", onHidden); manager.show("xxAddAgentModal", "idx_dlgOkButton", onAccept); } else submit.addEventListener("click", onAccept, true); }); }
    tools.setParameterOptionProvider = function (provider) { sharedOptionProvider = typeof provider === "function" ? provider : null; };
    tools.openParameterDialog = openParameterDialog;
    tools.openConfirmationDialog = openConfirmationDialog;
    tools.parameterDialogContract = { allowCustom: allowCustom, assetDependsOnUser: assetDependsOnUser, assetUserDependency: assetUserDependency, controlKind: controlKind, currentValues: currentValues, optionProvider: function () { return sharedOptionProvider; }, validate: validate };
    var createTools = tools.create;
    if (typeof createTools === "function") tools.create = function (options) { var instance = createTools(options); if (instance && typeof instance === "object") { instance.openParameterDialog = function (dialogOptions) { return tools.openParameterDialog(dialogOptions); }; instance.openConfirmationDialog = function (dialogOptions) { return tools.openConfirmationDialog(dialogOptions); }; instance.setParameterOptionProvider = function (provider) { return tools.setParameterOptionProvider(provider); }; } return instance; };
}());
