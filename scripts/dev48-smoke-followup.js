"use strict";
const fs = require("fs");

function replace(path, from, to) {
  const src = fs.readFileSync(path, "utf8");
  if (!src.includes(from)) throw new Error("Marker not found in " + path);
  fs.writeFileSync(path, src.replace(from, to), "utf8");
}

// Shared native confirmation dialog.
replace("public/shared/ui/parameter-dialog.js",
`    tools.setParameterOptionProvider = function (provider) {\n`,
`    function openConfirmationDialog(options) {\n        options = options || {};\n        var manager = hostDialogManager();\n        if (!manager) return Promise.reject(new Error("Native MeshCentral dialog manager is unavailable."));\n        var sequence = ++dialogSequence;\n        var prefix = "SirkConfirmationDialog" + sequence;\n        var content = document.createElement("div");\n        content.className = "mc-parameter-dialog-content";\n        var message = document.createElement("p");\n        message.className = "mc-shared-muted";\n        message.textContent = text(options.message || "Confirm execution.");\n        content.appendChild(message);\n        var title = text(options.title || "Confirm execution");\n        if (manager.mode === "modern") manager.setContent("xxAddAgent", title, content.innerHTML);\n        else manager.show(2, title, 3, null, content.innerHTML);\n        var submit = document.getElementById("idx_dlgOkButton");\n        var cancel = document.getElementById("idx_dlgCancelButton");\n        var close = document.getElementById("id_dialogclose");\n        if (!submit) return Promise.reject(new Error("Native MeshCentral confirmation controls are unavailable."));\n        var trigger = options.trigger || document.activeElement;\n        var primaryLabel = text(options.primaryLabel || "Run");\n        var originalSubmitText = readButtonText(submit);\n        var originalSubmitDisabled = !!submit.disabled;\n        var modernModal = document.getElementById("xxAddAgentModal");\n        var settled = false;\n        return new Promise(function (resolve) {\n            function cleanup() {\n                if (manager.mode === "classic") submit.removeEventListener("click", onAccept, true);\n                if (cancel) cancel.removeEventListener("click", onCancel, true);\n                if (close) close.removeEventListener("click", onCancel, true);\n                if (modernModal) modernModal.removeEventListener("hidden.bs.modal", onCancel);\n                writeButtonText(submit, originalSubmitText);\n                submit.disabled = originalSubmitDisabled;\n                if (trigger && trigger.isConnected !== false && typeof trigger.focus === "function") trigger.focus();\n            }\n            function finish(value) {\n                if (settled) return;\n                settled = true;\n                cleanup();\n                resolve(value);\n            }\n            function onAccept(event) {\n                if (event && manager.mode === "classic") {\n                    event.preventDefault();\n                    event.stopImmediatePropagation();\n                }\n                finish(true);\n            }\n            function onCancel() { finish(false); }\n            writeButtonText(submit, primaryLabel);\n            submit.disabled = false;\n            if (cancel) cancel.addEventListener("click", onCancel, true);\n            if (close) close.addEventListener("click", onCancel, true);\n            if (manager.mode === "modern") {\n                if (modernModal) modernModal.addEventListener("hidden.bs.modal", onCancel);\n                manager.show("xxAddAgentModal", "idx_dlgOkButton", function () { finish(true); return true; });\n            } else submit.addEventListener("click", onAccept, true);\n        });\n    }\n\n    tools.setParameterOptionProvider = function (provider) {\n`);
replace("public/shared/ui/parameter-dialog.js",
`    tools.openParameterDialog = openParameterDialog;\n`,
`    tools.openParameterDialog = openParameterDialog;\n    tools.openConfirmationDialog = openConfirmationDialog;\n`);
replace("public/shared/ui/parameter-dialog.js",
`                instance.openParameterDialog = function (dialogOptions) {\n                    return tools.openParameterDialog(dialogOptions);\n                };\n`,
`                instance.openParameterDialog = function (dialogOptions) {\n                    return tools.openParameterDialog(dialogOptions);\n                };\n                instance.openConfirmationDialog = function (dialogOptions) {\n                    return tools.openConfirmationDialog(dialogOptions);\n                };\n`);

// My Scripts: native dialogs own launch; no parallel third-column Run card on click.
replace("public/modules/automation/index.js",
`    function confirmExecution(script) {\n        if (!script || script.confirmExecution !== true) return true;\n        return window.confirm(\n            "Run \\\"" + (script.label || script.name || script.path || "this script") + "\\\" now?"\n        );\n    }\n`,
`    function confirmExecution(script, trigger) {\n        if (!script || script.confirmExecution !== true) return Promise.resolve(true);\n        if (!tools || typeof tools.openConfirmationDialog !== "function") {\n            return Promise.reject(new Error("Native MeshCentral confirmation dialog is unavailable."));\n        }\n        return tools.openConfirmationDialog({\n            title: script.label || script.name || "Confirm execution",\n            message: "Run \\\"" + (script.label || script.name || script.path || "this script") + "\\\" now?",\n            trigger: trigger,\n            primaryLabel: script.requiresApproval ? "Request" : "Run"\n        });\n    }\n`);
replace("public/modules/automation/index.js",
`    function submit(shell, script, button, values, detailsHost, resultHost, errorHost) {\n        if (!confirmExecution(script)) {\n            if (errorHost) showValidationError(shell, errorHost, new Error("Execution cancelled."));\n            else switchToResult(detailsHost, resultHost, "Execution cancelled.");\n            return;\n        }\n\n        if (button) button.disabled = true;\n        switchToResult(detailsHost, resultHost, "Executing script...");\n\n        var sequence = ++progressSequence;\n        var protocolPending = false;\n        shell.post("request", {\n            scriptPath: script.path,\n            variableValues: values || {},\n            confirmedExecution: script.confirmExecution === true,\n            note: ""\n        }).then(function (response) {\n            var request = response.request || {};\n            outputs[script.path] = request;\n            if (response.protocol === true && request.id) {\n                protocolPending = true;\n                pollProtocol(shell, script, request, resultHost, sequence, 0, button);\n            } else renderResult(resultHost, request);\n        }).catch(function (error) {\n            var request = {\n                status: "failed",\n                title: script.label || script.name,\n                result: { message: error.message || String(error) }\n            };\n            outputs[script.path] = request;\n            renderResult(resultHost, request);\n        }).then(function () {\n            if (button && !protocolPending) button.disabled = false;\n            sync(shell);\n        });\n    }\n`,
`    function submit(shell, script, button, values, detailsHost, resultHost, errorHost) {\n        return confirmExecution(script, button || document.activeElement).then(function (confirmed) {\n            if (!confirmed) return;\n            if (button) button.disabled = true;\n            switchToResult(detailsHost, resultHost, "Executing script...");\n\n            var sequence = ++progressSequence;\n            var protocolPending = false;\n            return shell.post("request", {\n                scriptPath: script.path,\n                variableValues: values || {},\n                confirmedExecution: script.confirmExecution === true,\n                note: ""\n            }).then(function (response) {\n                var request = response.request || {};\n                outputs[script.path] = request;\n                if (response.protocol === true && request.id) {\n                    protocolPending = true;\n                    pollProtocol(shell, script, request, resultHost, sequence, 0, button);\n                } else renderResult(resultHost, request);\n            }).catch(function (error) {\n                var request = { status: "failed", title: script.label || script.name, result: { message: error.message || String(error) } };\n                outputs[script.path] = request;\n                renderResult(resultHost, request);\n            }).then(function () {\n                if (button && !protocolPending) button.disabled = false;\n                sync(shell);\n            });\n        }).catch(function (error) {\n            if (errorHost) showValidationError(shell, errorHost, error);\n            else switchToResult(detailsHost, resultHost, error.message || String(error));\n        });\n    }\n`);
replace("public/modules/automation/index.js",
`            var card = shell.card(script.label || script.name, script.description || script.path);\n`,
`            if (executeOnSelect === true && hasVariables) {\n                sync(shell);\n                if (!tools || typeof tools.openParameterDialog !== "function") {\n                    switchToResult(detailsHost, resultHost, "Native MeshCentral parameter dialog is unavailable.");\n                    resultHost.classList.add("mc-shared-error");\n                    return;\n                }\n                tools.openParameterDialog({\n                    item: script, trigger: document.activeElement,\n                    primaryLabel: script.requiresApproval ? "Request" : "Run"\n                }).then(function (values) {\n                    if (values == null) return;\n                    submit(shell, script, null, values, detailsHost, resultHost, null);\n                }).catch(function (error) {\n                    switchToResult(detailsHost, resultHost, error.message || String(error));\n                    resultHost.classList.add("mc-shared-error");\n                });\n                return;\n            }\n\n            var card = shell.card(script.label || script.name, script.description || script.path);\n`);
replace("public/modules/automation/index.js",
`            if (executeOnSelect === true && hasVariables) button.click();\n`, "");

// My Commands confirmation uses the same native dialog.
replace("public/modules/commands/index.js",
`    function confirmExecution(item) { if (!item || item.confirmExecution !== true) return true; return window.confirm(msg("Uruchomić teraz: ", "Run now: ") + (item.label || item.name || item.path) + "?"); }\n`,
`    function confirmExecution(item, trigger) {\n        if (!item || item.confirmExecution !== true) return Promise.resolve(true);\n        if (!tools || typeof tools.openConfirmationDialog !== "function") return Promise.reject(new Error("Native MeshCentral confirmation dialog is unavailable."));\n        return tools.openConfirmationDialog({\n            title: item.label || item.name || msg("Potwierdź wykonanie", "Confirm execution"),\n            message: msg("Uruchomić teraz: ", "Run now: ") + (item.label || item.name || item.path) + "?",\n            trigger: trigger,\n            primaryLabel: item.requiresApproval ? msg("Poproś o akceptację", "Request") : msg("Uruchom", "Run")\n        });\n    }\n`);
replace("public/modules/commands/index.js",
`    function execute(shell, item, button, values, outputHost) {\n        if (!confirmExecution(item)) { renderWaiting(outputHost, msg("Anulowano wykonanie.", "Execution cancelled.")); return; }\n        if (button) button.disabled = true;\n        outputHost.classList.remove("mc-shared-error"); renderWaiting(outputHost, msg("Wysyłanie polecenia…", "Submitting command…"));\n        var payload = { nodeId: node(shell), nodeName: window.currentNode && window.currentNode.name || "", variableValues: values || {}, confirmedExecution: item.confirmExecution === true, note: "" };\n        if (item.kind === "command") payload.commandId = item.commandId; else payload.scriptPath = item.path;\n        shell.post("execute", payload).then(function (response) {\n            var request = response.request || {}, result = request.result || {};\n            if (request.status === "pending") { outputs[item.path] = msg("Oczekiwanie na akceptację.", "Waiting for approval."); renderWaiting(outputHost, outputs[item.path]); return; }\n            if (result.id) { var immediate = result.output || result.message || msg("Oczekiwanie na wynik agenta…", "Waiting for agent output…"); outputs[item.path] = immediate; renderWaiting(outputHost, immediate); pollSequence++; pollOutput(shell, item, result.id, outputHost, pollSequence, 0); return; }\n            var value = result.output || result.message || request.status || msg("Polecenie wysłane.", "Command submitted."); outputs[item.path] = stringValue(value); renderOutput(outputHost, outputs[item.path]);\n        }).catch(function (error) { outputs[item.path] = error.message || String(error); renderWaiting(outputHost, outputs[item.path]); outputHost.classList.add("mc-shared-error"); }).then(function () { if (button) button.disabled = false; });\n    }\n`,
`    function execute(shell, item, button, values, outputHost) {\n        return confirmExecution(item, button || document.activeElement).then(function (confirmed) {\n            if (!confirmed) return;\n            if (button) button.disabled = true;\n            outputHost.classList.remove("mc-shared-error"); renderWaiting(outputHost, msg("Wysyłanie polecenia…", "Submitting command…"));\n            var payload = { nodeId: node(shell), nodeName: window.currentNode && window.currentNode.name || "", variableValues: values || {}, confirmedExecution: item.confirmExecution === true, note: "" };\n            if (item.kind === "command") payload.commandId = item.commandId; else payload.scriptPath = item.path;\n            return shell.post("execute", payload).then(function (response) {\n                var request = response.request || {}, result = request.result || {};\n                if (request.status === "pending") { outputs[item.path] = msg("Oczekiwanie na akceptację.", "Waiting for approval."); renderWaiting(outputHost, outputs[item.path]); return; }\n                if (result.id) { var immediate = result.output || result.message || msg("Oczekiwanie na wynik agenta…", "Waiting for agent output…"); outputs[item.path] = immediate; renderWaiting(outputHost, immediate); pollSequence++; pollOutput(shell, item, result.id, outputHost, pollSequence, 0); return; }\n                var value = result.output || result.message || request.status || msg("Polecenie wysłane.", "Command submitted."); outputs[item.path] = stringValue(value); renderOutput(outputHost, outputs[item.path]);\n            }).catch(function (error) { outputs[item.path] = error.message || String(error); renderWaiting(outputHost, outputs[item.path]); outputHost.classList.add("mc-shared-error"); }).then(function () { if (button) button.disabled = false; });\n        }).catch(function (error) {\n            outputs[item.path] = error.message || String(error);\n            renderWaiting(outputHost, outputs[item.path]);\n            outputHost.classList.add("mc-shared-error");\n        });\n    }\n`);
replace("public/modules/commands/index.js",
`    function multi(shell, item) {\n        if (!confirmExecution(item)) return;\n        parameterValues(item, document.activeElement).then(function (values) {\n`,
`    function multi(shell, item) {\n        var trigger = document.activeElement;\n        confirmExecution(item, trigger).then(function (confirmed) {\n            if (!confirmed) return null;\n            return parameterValues(item, trigger);\n        }).then(function (values) {\n`);

// Credentials editor: include existing system-profile assignment alongside script-local secrets.
const scriptTools = fs.readFileSync("public/shared/ui/script-tools.js", "utf8");
const start = scriptTools.indexOf("            function openCredentialsEditor(shell, script, onSaved) {");
const end = scriptTools.indexOf("\n            function openMultiExecution", start);
if (start < 0 || end < 0) throw new Error("Credentials editor bounds not found");
const replacement = `            function openCredentialsEditor(shell, script, onSaved) {\n                Promise.all([\n                    shell.api("script-secrets", { path: script.path }),\n                    shell.api("system-credentials", { path: script.path })\n                ]).then(function (responses) {\n                    var value = responses[0].secrets || {};\n                    var systemValue = responses[1].systemCredentials || { profiles: [] };\n                    var host = shell.state.page.details;\n                    host.innerHTML = "";\n                    var card = shell.card("Script credentials", script.label || script.name);\n                    card.classList.add("mc-script-credentials-card");\n\n                    var system = element("section", "mc-definition-section mc-definition-system-credentials");\n                    system.appendChild(element("h4", "", "System credentials"));\n                    system.appendChild(element("div", "mc-shared-muted", "Assign globally configured profiles to this script. Configure missing profile values in Settings > Integrations."));\n                    var profileControls = [];\n                    (systemValue.profiles || []).forEach(function (profile) {\n                        var label = element("label", "mc-definition-check");\n                        var input = document.createElement("input");\n                        input.type = "checkbox";\n                        input.value = profile.name;\n                        input.checked = profile.selected === true;\n                        input.disabled = profile.configured !== true;\n                        label.appendChild(input);\n                        label.appendChild(document.createTextNode(" " + profile.label + " - " + (profile.configured ? "configured" : "not configured")));\n                        system.appendChild(label);\n                        profileControls.push({ profile: profile, input: input });\n                    });\n                    card.appendChild(system);\n\n                    var local = element("section", "mc-definition-section");\n                    local.appendChild(element("h4", "", "Script-local secrets"));\n                    var controls = [];\n                    (value.variables || []).forEach(function (variable) {\n                        var group = document.createElement("div"); group.className = "mc-script-secret-row";\n                        var input = document.createElement("input"); input.type = "password"; input.autocomplete = "new-password";\n                        input.placeholder = variable.configured ? "Configured - leave empty to keep" : "Enter secret";\n                        var clear = document.createElement("input"); clear.type = "checkbox";\n                        var clearLabel = document.createElement("label"); clearLabel.appendChild(clear); clearLabel.appendChild(document.createTextNode(" Clear saved value"));\n                        var status = document.createElement("span"); status.className = variable.configured ? "mc-secret-configured" : "mc-secret-missing";\n                        status.textContent = variable.configured ? "Configured" : (variable.required ? "Required" : "Not configured");\n                        group.appendChild(formRow(variable.label + (variable.required ? " *" : ""), input)); group.appendChild(status); group.appendChild(clearLabel);\n                        local.appendChild(group); controls.push({ variable: variable, input: input, clear: clear });\n                    });\n                    if (!controls.length) local.appendChild(element("div", "mc-shared-muted", "This script has no script-local SaveSecret directives."));\n                    card.appendChild(local);\n\n                    var save = shell.element("button", "btn btn-primary btn-sm", "Save credentials"); save.type = "button";\n                    save.onclick = function () {\n                        var values = {}, clearNames = [];\n                        controls.forEach(function (item) { if (item.input.value) values[item.variable.name] = item.input.value; if (item.clear.checked) clearNames.push(item.variable.name); });\n                        var selected = profileControls.filter(function (item) { return item.input.checked && !item.input.disabled; }).map(function (item) { return item.profile.name; });\n                        save.disabled = true;\n                        Promise.all([\n                            shell.post("script-secrets", { path: script.path, values: values, clearNames: clearNames }),\n                            shell.post("system-credentials", { path: script.path, selected: selected })\n                        ]).then(function (result) { if (typeof onSaved === "function") onSaved(result[0]); }).catch(function (error) { save.disabled = false; shell.error(host, error); });\n                    };\n                    card.appendChild(save); host.appendChild(card);\n                }).catch(function (error) { shell.error(shell.state.page.details, error); });\n            }\n`;
fs.writeFileSync("public/shared/ui/script-tools.js", scriptTools.slice(0, start) + replacement + scriptTools.slice(end), "utf8");

// Integrations: closed disclosure sections Jira + AD + AAD/Entra, one canonical save.
fs.writeFileSync("web/admin/integrations.js", `(function () {\n    "use strict";\n    var root = document.getElementById("sirk-platform-admin");\n    var content = document.getElementById("sirk-platform-admin-content");\n    var button = root && root.querySelector('[data-tab="integrations"]');\n    if (!root || !content || !button) return;\n    function data() { return window.SirkPlatformAdminData || {}; }\n    function element(tag, className, text) { var n=document.createElement(tag); if(className)n.className=className; if(text!=null)n.textContent=String(text); return n; }\n    function disclosure(host,title){ var d=element("details","mc-admin-disclosure"); d.open=false; d.appendChild(element("summary","mc-admin-disclosure-summary",title)); host.appendChild(d); return d; }\n    function field(host,labelText,value,options){ options=options||{}; var label=element("label","mc-admin-field"); label.appendChild(element("span","mc-admin-field-label",labelText)); var input=document.createElement(options.multiline?"textarea":"input"); input.className="mc-admin-input"; if(!options.multiline)input.type=options.type||"text"; input.value=value==null?"":String(value); if(options.placeholder)input.placeholder=options.placeholder; if(options.multiline)input.rows=options.rows||3; label.appendChild(input); host.appendChild(label); return input; }\n    function checkbox(host,labelText,checked){ var label=element("label","mc-admin-check"), input=document.createElement("input"); input.type="checkbox"; input.checked=checked===true; label.appendChild(input); label.appendChild(document.createTextNode(labelText)); host.appendChild(label); return input; }\n    function clone(value){ try{return JSON.parse(JSON.stringify(value||{}));}catch(e){return{};} }\n    function pluginUrl(action){ var url=new URL("pluginadmin.ashx",window.location.href); url.searchParams.set("pin",root.getAttribute("data-plugin")||"SIRKPortal"); url.searchParams.set("action",action); return url.href; }\n    function activate(){ root.querySelectorAll("[data-tab]").forEach(function(item){item.classList.toggle("active",item===button);}); render(); }\n    function render(){\n        content.innerHTML=""; var card=element("section","mc-admin-card"); card.appendChild(element("h3","","Integrations")); card.appendChild(element("p","mc-admin-card-description","Global system credentials used by Script credentials assignments. Secrets are write-only and remain encrypted server-side.")); content.appendChild(card);\n        var snapshot=data().integrations||{}, values=snapshot.values||{}, configured=snapshot.configured||{};\n        var jira=values.jira||{}, ad=values.ad||{}, entra=values.entra||{};\n        var jiraBox=disclosure(card,"Jira");\n        var jiraUrl=field(jiraBox,"Jira URL",jira.url||"",{placeholder:"https://tenant.atlassian.net"});\n        var jiraEmail=field(jiraBox,"Jira account email",jira.email||"");\n        var jiraToken=field(jiraBox,"Jira API token","",{type:"password",placeholder:configured.jiraToken?"Configured - leave blank to keep":"Required"}); jiraToken.autocomplete="new-password";\n        var projectKey=field(jiraBox,"Project key",jira.projectKey||""); var workspaceId=field(jiraBox,"Assets workspace ID",jira.workspaceId||""); var cloudId=field(jiraBox,"Cloud ID",jira.cloudId||""); var hostnameAttribute=field(jiraBox,"Hostname attribute",jira.hostnameAttribute||"Hostname"); var assetFieldId=field(jiraBox,"Asset field ID",jira.assetFieldId||""); var aql=field(jiraBox,"Assets AQL scope",jira.aql||"objectType = Computer",{multiline:true}); var verifyTls=checkbox(jiraBox,"Verify Jira TLS certificate",jira.verifyTls!==false);\n        var adBox=disclosure(card,"Active Directory"); var adDomain=field(adBox,"AD domain",ad.domain||""); var adLogin=field(adBox,"AD login",ad.login||""); var adPassword=field(adBox,"AD password","",{type:"password",placeholder:configured.adPassword?"Configured - leave blank to keep":"Required"}); adPassword.autocomplete="new-password";\n        var entraBox=disclosure(card,"AAD / Entra ID"); var tenantId=field(entraBox,"Tenant ID",entra.tenantId||""); var clientId=field(entraBox,"Client ID",entra.clientId||""); var clientSecret=field(entraBox,"Client secret","",{type:"password",placeholder:configured.entraClientSecret?"Configured - leave blank to keep":"Required"}); clientSecret.autocomplete="new-password";\n        var actions=element("div","mc-admin-actions"), save=element("button","mc-admin-primary","Save integrations"), status=element("span","mc-admin-save-status",""); save.type="button"; actions.appendChild(save); actions.appendChild(status); card.appendChild(actions);\n        save.onclick=function(){\n            save.disabled=true; status.textContent="Saving..."; var integrations=clone(values); integrations.jira=Object.assign({},jira,{url:jiraUrl.value,email:jiraEmail.value,projectKey:projectKey.value,workspaceId:workspaceId.value,cloudId:cloudId.value,hostnameAttribute:hostnameAttribute.value,assetFieldId:assetFieldId.value,aql:aql.value,verifyTls:verifyTls.checked}); integrations.ad=Object.assign({},ad,{domain:adDomain.value,login:adLogin.value}); integrations.entra=Object.assign({},entra,{tenantId:tenantId.value,clientId:clientId.value}); var secrets={}; if(jiraToken.value)secrets.jiraToken=jiraToken.value; if(adPassword.value)secrets.adPassword=adPassword.value; if(clientSecret.value)secrets.entraClientSecret=clientSecret.value; var body=new URLSearchParams(); body.set("action","save-integrations"); body.set("integrations",JSON.stringify(integrations)); body.set("secrets",JSON.stringify(secrets));\n            fetch(pluginUrl("save-integrations"),{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/x-www-form-urlencoded; charset=UTF-8","Accept":"application/json"},body:body.toString()}).then(function(response){return response.json().then(function(result){if(!response.ok||!result.ok)throw new Error(result.error||"Save failed.");return result;});}).then(function(result){window.SirkPlatformAdminData.integrations=result.integrations; status.textContent="Saved"; jiraToken.value=adPassword.value=clientSecret.value="";}).catch(function(error){status.className="mc-admin-save-status mc-admin-error"; status.textContent=error.message||String(error);}).then(function(){save.disabled=false;});\n        };\n    }\n    button.onclick=activate;\n}());\n`, "utf8");

// Regression tests for the new shared UX contract.
fs.writeFileSync("test/dev48-smoke-followup.test.js", `"use strict";\nvar assert=require("assert"),fs=require("fs"),path=require("path"),root=path.resolve(__dirname,"..");\nfunction read(p){return fs.readFileSync(path.join(root,p),"utf8");}\nvar dialog=read("public/shared/ui/parameter-dialog.js"), scripts=read("public/modules/automation/index.js"), commands=read("public/modules/commands/index.js"), tools=read("public/shared/ui/script-tools.js"), integrations=read("web/admin/integrations.js");\nassert.ok(dialog.indexOf("function openConfirmationDialog(options)")>=0 && dialog.indexOf("instance.openConfirmationDialog")>=0,"Shared tools instances must expose native confirmation dialog.");\nassert.strictEqual(scripts.indexOf("window.confirm("),-1,"My Scripts must not use browser confirm.");\nassert.strictEqual(commands.indexOf("window.confirm("),-1,"My Commands must not use browser confirm.");\nassert.ok(scripts.indexOf("if (executeOnSelect === true && hasVariables)")>=0 && scripts.indexOf("item: script, trigger: document.activeElement")>=0,"Parameterized My Scripts click must open native dialog directly before rendering launch card.");\nassert.ok(tools.indexOf('shell.api("system-credentials"')>=0 && tools.indexOf('shell.post("system-credentials"')>=0,"Script credentials must expose system-profile assignment.");\n["Jira","Active Directory","AAD / Entra ID"].forEach(function(label){assert.ok(integrations.indexOf('disclosure(card,"'+label+'")')>=0,"Missing closed integration section: "+label);});\nassert.ok(integrations.indexOf('secrets.jiraToken')>=0 && integrations.indexOf('secrets.adPassword')>=0 && integrations.indexOf('secrets.entraClientSecret')>=0,"Integration secrets must remain write-only save values.");\nconsole.log("dev48 smoke follow-up UX contract: OK");\n`, "utf8");

// Version + dev notes.
["package.json","config.json"].forEach(function(p){ var s=fs.readFileSync(p,"utf8"); s=s.replace(/0\\.1\\.1-dev\\.47/g,"0.1.1-dev.48"); fs.writeFileSync(p,s,"utf8"); });
["README.md","docs/PROJECT-STATE.md"].forEach(function(p){ var s=fs.readFileSync(p,"utf8").replace(/0\\.1\\.1-dev\\.47/g,"0.1.1-dev.48"); fs.writeFileSync(p,s,"utf8"); });
var changelog=fs.readFileSync("changelog.md","utf8"); fs.writeFileSync("changelog.md",`## 0.1.1-dev.48 - 2026-08-10\n\n- Real dev.47 smoke follow-up: Script credentials now exposes system profile assignments; Integrations use closed Jira/AD/Entra sections; My Scripts parameter launch no longer leaves a parallel third-column Run card; confirmation uses native MeshCentral dialog in My Scripts/My Commands.\n- Network Settings #128 remains open pending execution-context evidence; no additional blind launcher workaround in this candidate.\n- No tag/GitHub Release.\n\n`+changelog,"utf8");
var release=`# MC-SIRK 0.1.1-dev.48\n\nStatus: development pre-1.0 candidate for real MeshCentral smoke.\n\n## Changes\n- #280: parameterized My Scripts click opens the native dialog as the only launch surface; shared native confirmation replaces browser confirm in My Scripts/My Commands.\n- #281: Script credentials includes system-profile assignment; Settings > Integrations has closed Jira, Active Directory and AAD / Entra ID sections with write-only secrets.\n- #252 remains open for real Jira protocol flow verification after credentials are configured/assigned.\n- #128 remains open; Network Settings still requires real-host execution-context evidence before another launcher change.\n\n## Smoke\n1. Script credentials -> assign Jira to Jira Asset Protocol; configure missing Jira values in Settings > Integrations.\n2. Click a parameterized My Script: native dialog only; no parallel Run card.\n3. Enable Confirm before execution and run: native modal, not browser alert.\n4. Jira Asset Protocol: user -> asset -> operation/person -> PDF.\n5. Network Settings: report only whether properties opens; if not, capture diagnostic next.\n\nNo tag/GitHub Release.\n`; fs.writeFileSync("docs/releases/0.1.1-dev.48.md",release,"utf8");
var releases=fs.readFileSync("docs/releases/README.md","utf8"); if(!releases.includes("0.1.1-dev.48")) releases=releases.replace("Aktualna linia development:\n\n","Aktualna linia development:\n\n- [`0.1.1-dev.48`](0.1.1-dev.48.md) - dev.47 smoke follow-up: Script credentials/system profiles, closed Jira/AD/Entra settings, native launch/confirmation dialogs;\n"); fs.writeFileSync("docs/releases/README.md",releases,"utf8");
var history=JSON.parse(fs.readFileSync("version-history.json","utf8")); if(Array.isArray(history)&&!history.some(function(x){return x.version==="0.1.1-dev.48";})) history.unshift({version:"0.1.1-dev.48",date:"2026-08-10",notes:"Script credentials/system profiles, closed Jira/AD/Entra settings, native parameter/confirmation launch UX."}); fs.writeFileSync("version-history.json",JSON.stringify(history,null,2)+"\n","utf8");
console.log("dev48 patch applied");
