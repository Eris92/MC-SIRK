# SIRK Management Platform 1.7.37

- Makes Approval Center and My Scripts reuse the exact native MeshCentral left-menu base class.
- Replaces the wider `lbbuttonsel2` state with the native `lbbuttonsel` state used by built-in entries.
- Preserves the native `.lbtg lb2` icon host instead of replacing it with a plugin-specific class structure.
- Keeps module SVG icons centered at a fixed native-sized `40px x 40px` inside the original `62px x 62px` host.
- Prevents repeated menu synchronization from duplicating or replacing the outer menu entry.
- Keeps native Devices menu classes and icon structure unchanged.
- Adds a functional DOM regression test for class identity, icon geometry, active state and repeated synchronization.
