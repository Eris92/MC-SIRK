# MC-SIRK 1.8.7

## Quick Output attention

- Synchronize the actual `is-details-collapsed` state with `mc-sirk-quickcommands-output-hidden-v2`.
- Detect hidden Output from both the rendered Quick layout and the compatibility data attribute.
- Persist attention in `mc-sirk-quickcommands-output-attention-v2` so the native theme lifecycle and the Quick controller cannot overwrite each other.
- Mark the Output button when a new final result appears while the third column is hidden.
- Clear attention immediately after Output is opened or its content is cleared.
- Keep loading, command-sent and command-list-refresh messages neutral.
- Continue using MeshCentral Bootstrap danger tokens instead of a fixed plugin palette.

## Toolbar geometry

- Add a 10 px bottom gutter below the Quick toolbar.
- Start the column divider lines below the toolbar controls instead of directly against the buttons.
- Preserve the existing left, right and close-button padding.

## Validation

- Extend the Quick attention contract test with both hidden-state controllers.
- Verify persistent attention synchronization and reset behavior.
- Verify the toolbar bottom gutter.
