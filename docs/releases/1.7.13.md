# SIRK Management Platform 1.7.13

- Synchronizes the Quick output button `is-active` class and `aria-pressed` value with the canonical output visibility state.
- Removes the highlighted state immediately when the result pane is already hidden on first render or after a remount.
- Restores the active state immediately when the result pane is opened.
- Extends the functional regression test to start from the exact conflicting state observed in the live interface.
