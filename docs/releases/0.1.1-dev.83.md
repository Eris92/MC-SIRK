# SIRK Management Platform 0.1.1-dev.83

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral/Jira acceptance pending

## Scope

Correct only the Jira protocol PDF execution path after the `0.1.1-dev.82` rollback restored the single unguarded Edge `--headless=new` invocation and real smoke again failed without creating a document.

The user-directed `0.1.1-dev.82` Jira equipment matching, pagination, caches, scripts and UI remain unchanged.

## PDF execution

- Give each render an isolated writable Edge profile inside the existing temporary render directory.
- Use portable forward-slash profile and PDF output paths and run Edge from that writable directory.
- Prefer compatibility `--headless`, then retry exactly once with `--headless=new`.
- Preserve bounded timeout, output size, diagnostics, `%PDF-1.` validation and cleanup.
- If both browser attempts fail in the MeshCentral Windows service context, generate one dependency-free text PDF from the canonical protocol text and surface the bounded browser failure reason in Results.

## Verification

- JavaScript syntax checks for both changed runtime owners.
- Targeted Edge invocation/retry/fallback regression.
- Targeted Jira protocol fallback artifact regression.
- Real local Edge render returned a valid styled PDF without using fallback.
- Full `npm test` required before integration.

## Version

`0.1.1-dev.82 -> 0.1.1-dev.83`.

No tag or GitHub Release is created.
