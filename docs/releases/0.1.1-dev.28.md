# MC-SIRK 0.1.1-dev.28

Status: development pre-1.0. No tag or GitHub Release.

## Runtime included

- Delivers Issue #213 from PR #222 / main commit `cf9f201e7c28dfa438d86a3d1bc031223729f28a` so MeshCentral update detection installs the larger second-column shared navigation icons.
- Shared/Quick item-identifying icons use one `28x28` slot with `24x24` SVG artwork across My Scripts, My Commands, Approval Center and Quick.
- First-column Collapse keeps the existing #155 `28/24` icon contract and 44 px row geometry; action-rail icons/buttons remain outside the larger list-icon contract.
- Neutral icon color from #120, selected-state treatment from #115/#147, custom image aspect ratio and existing runtime/request lifecycle remain unchanged.
- No JavaScript measurement, observer, timer, polling loop, additional render owner or request was added.

## Verification

- PR #222 canonical `Test` run `31303716169`: GREEN.
- Targeted regressions updated: `test/shared-list-quick-style.test.js` and `test/stable-first-column-icon-size.test.js`.
- Negative coverage keeps `.mc-tree-script-action-icon` outside `sirk-shared-list-icon` and preserves action-button geometry.
- Direct-consumer inspection confirms My Commands uses `SharedDirectoryTree`/`sirk-shared-list-icon`, Approval uses the shared list/icon classes, and Quick uses `sirk-quick-command-icon` for SVG and bitmap artwork.
- Real MeshCentral Classic/Modern + light/dark + wide/narrow smoke remains required by Issue #213 before closing it.

## Versioning

- Development revision only: `0.1.1-dev.27` -> `0.1.1-dev.28`.
- Active version sources: `package.json` and `config.json`.
- Version remains below `1.0.0`.
- No tag or GitHub Release.
