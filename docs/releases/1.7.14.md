# SIRK Management Platform 1.7.14

- Applies the canonical Quick output visibility state before the toolbar is appended to the DOM.
- Prevents the output icon from flashing during category changes, script selection and toolbar remounts.
- Neutralizes the superseded post-render output controller before it can issue a programmatic second click.
- Keeps the output button title, `is-active` class, `aria-pressed` value and hidden panel geometry synchronized in the same render task.
- Adds a functional regression test for conflicting canonical and legacy states during remounts.
