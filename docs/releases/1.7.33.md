# SIRK Management Platform 1.7.33

- Captures the actual rendered first-column, second-column and script-row widths before Edit or Multi changes the layout.
- Keeps the first column pixel-identical when Edit is enabled.
- Keeps the script text area pixel-identical so labels wrap exactly as they did before Edit.
- Expands only the second column by the fixed action-button width and gap.
- Prevents later toolbar synchronization or rerenders from recapturing the already expanded Edit layout.
- Releases captured widths when the last active mode is closed.
- Applies the same live-width contract to My Scripts and Commands while keeping My Scripts single-device-only.
- Keeps the Edit/Multi toolbar switch introduced in 1.7.32.
- Adds functional regression coverage using explicit 218 px / 306 px / 282 px rendered geometry.
