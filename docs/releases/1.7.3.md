# SIRK Management Platform 1.7.3

- Keep the Quick Commands output column hidden until the user opens it explicitly.
- When a hidden command result or refresh result arrives, mark the output toolbar button with a subtle red attention state; clear it after opening the output pane.
- Restore the Commands device page after F5 by reactivating the registered plugin page and remounting My Commands for the current node.
- Keep Commands and the native Plugins tab separate, including their active state and stored plugin page.
- Display `Commands - <device>` while the Commands tab is active and restore the native Plugins title on exit.
- Remove the nested Plugins subheader from Commands and align SIRK page headings with native MeshCentral page headings.
