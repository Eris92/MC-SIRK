# SIRK Management Platform 1.6.78

- Use MeshCentral's actual legacy selected class `lbbuttonsel2` for the active left-menu item.
- Clear stale native menu selection globally instead of only among direct siblings.
- Re-apply the active SIRK menu after delayed native page rendering and later class changes.
- Keep modern navigation support through the `active` class and `aria-current=page`.
