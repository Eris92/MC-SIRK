# MC-SIRK 1.8.12

## Approval Center

- `Approve` now uses the native MeshCentral connect-button contract: `btn btn-success btn-sm`.
- `Reject` now uses the native MeshCentral disconnect-button contract: `btn btn-danger btn-sm`.
- Existing semantic classes and decision behavior remain unchanged.
- The same styling is used in approval cards and table actions.

## Validation

- Added a regression test that requires the exact native Bootstrap classes.
