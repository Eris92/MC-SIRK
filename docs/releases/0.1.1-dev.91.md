# SIRK Management Platform 0.1.1-dev.91

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral acceptance pending

## Scope

Stop Jira protocol PDFs from opening automatically when generation completes.

## Correction

- Create new protocol PDF artifacts with `autoOpen: false`.
- Remove the frontend automatic artifact-opening state and completion handler.
- Ignore the legacy `autoOpen` flag on historical protocol results.
- Preserve the protected `Open PDF` and `Download PDF` actions without changing their URLs or permissions.

## Verification

- Jira protocol lifecycle regression requires `autoOpen: false` and absence of the frontend auto-open handler.
- Typed artifact download and Results button regressions pass.
- JavaScript syntax and `git diff --check` pass.
- Full `npm test` required before integration.

## Version

`0.1.1-dev.90 -> 0.1.1-dev.91`.

No tag or GitHub Release is created.
