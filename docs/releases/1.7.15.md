# SIRK Management Platform 1.7.15

- Keeps exactly one click owner for the Quick output visibility button.
- Prevents `quick-output-state.js` from wrapping a button already controlled by the Quick toolbar.
- Restores the full `hidden -> visible -> hidden` sequence with one click per transition.
- Preserves the pre-paint state synchronization from 1.7.14, so the icon does not flash during remounts.
- Adds a combined regression test for `toolbar-api.js` and `quick-output-state.js` ownership.
