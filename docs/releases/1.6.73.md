# SIRK Management Platform 1.6.73

- Resolve native Commands execution against the device currently open in MeshCentral.
- Prefer the canonical `currentNode._id` over stale plugin state or `selectedNode` values.
- Reject execution locally when no canonical `node/<domain>/<id>` identifier can be determined.
- Preserve the authenticated user's normal MeshCentral device rights checks.
