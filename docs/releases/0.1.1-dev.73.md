# SIRK Management Platform 0.1.1-dev.73

Status: development pre-1.0.

## Changes

- Resolve Jira Assets assignments that reference a workspace `Users`/person identity object instead of embedding the Jira account directly on the equipment object.
- Derive bounded identity aliases (Users object id/key/label) from the existing workspace snapshot only when a user-like identity object matches the selected Jira account.
- Keep direct Jira user references supported and keep unrelated plain text outside user-like identity objects from becoming assignment matches.
- Exclude the selected Jira identity object itself from the equipment selector.

## Verification

PR #321 candidate Actions run `31610603012`: full `npm test` GREEN and Windows interactive-shell smoke GREEN.

No tag or GitHub Release. Product remains below `1.0.0`.
