# SIRK Management Platform 1.6.70

- Run file-backed Commands scripts configured as Logged-on user through the active interactive Windows session.
- Resolve the effective account from the active WTS session and `explorer.exe` instead of inheriting the MeshAgent SYSTEM profile.
- Preserve user environment variables such as `APPDATA` and `USERPROFILE`.
- Capture command output and exit code from the user-session task and return them to MeshCentral.
- Keep SYSTEM scripts unchanged and route legacy `runAsUser: 1` through the same reliable user-session path.
