# 0.1.132

## Scope

- Keep the full accumulated execution output available only in `Debug / raw output`.
- Make the normal Results body for generated CSV reports show only the current downloadable report line and the latest record count.
- Replace the full server filesystem path in the visible report line with the CSV basename; the authenticated download still uses the full latest `CSV_DOWNLOAD:` path internally.
- Preserve structured CSV result bodies that use a marker plus JSON/table output and preserve all non-CSV result rendering unchanged.
- Reuse the existing `public/shared/ui/results.js` owner; no renderer, observer, timer or polling loop added.

## Verification

- Generated-download regression covers accumulated historical CSV output, newest download marker, concise current summary, raw/debug preservation and structured-output compatibility.
- Full `npm test` required because the shared Results contract changes.

Issue #400 remains open until real MeshCentral acceptance confirms the normal Results pane no longer shows stale historical report blocks.
