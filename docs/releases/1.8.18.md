# MC-SIRK 1.8.18

- naprawiono wejście do widoku `Wyniki` w Commands;
- przycisk Results używa niezależnego listenera `click` w fazie capture;
- kliknięcie od razu synchronizuje `active`, `is-active`, `aria-selected` i `data-sirk-list-selected`;
- nawigacja pozostaje aktywna po ponownym renderze rootów i po nadpisaniu właściwości `onclick` przez późniejszą warstwę UI;
- dodano funkcjonalny test rzeczywistego kliknięcia Results.
