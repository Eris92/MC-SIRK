# SIRK Management Platform 0.1.1-dev.95

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral acceptance pending

## Scope

Make the Jira protocol complete its 24-hour cache freshness checks before displaying data-dependent wizard lists.

## Correction

- Resolve the Jira user option provider before opening the first protocol dialog.
- Reuse the prepared user options inside the dialog instead of issuing a second request.
- Preserve the existing user-scoped equipment prefetch before opening the equipment dialog.
- Keep the authoritative backend recheck when the protocol is executed.
- Preserve the existing stale-cache fallback when Jira is temporarily unavailable.

## Verification

- The wizard regression requires exactly one user provider call to finish before the first dialog opens.
- Jira wizard, asset option and protocol runtime regressions pass.
- `git diff --check` passes.
- Full `npm test` required before integration.

## Version

`0.1.1-dev.94 -> 0.1.1-dev.95`.

No tag or GitHub Release is created.
