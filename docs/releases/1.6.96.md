# SIRK Management Platform 1.6.96

- Make the Quick commands output box fill the available third pane width and height.
- Give command output its own internal scrollbar instead of scrolling the whole details column.
- Keep variable forms compact while allowing output to use the complete details pane.
- Shrink the entire Quick commands panel when the first column is collapsed so no empty strip remains on the right.
- Preserve responsive stacking on narrow screens.
