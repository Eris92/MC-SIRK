# SIRK Management Platform 1.7.29

- Widens the Commands script column in Edit mode by a fixed amount reserved for the four row-action buttons.
- Keeps a usable script-label width instead of squeezing descriptions into a narrow strip.
- Preserves the compact root navigation width when the left column is collapsed.
- Uses a smaller fixed Edit width on narrower desktop viewports and the normal stacked layout on mobile widths.
- Makes Edit and Multi mutually exclusive in both directions.
- Makes Multi show only the multi-device action and Edit show only the Edit action set.
- Adds functional regression coverage for Edit → Multi → Edit transitions.
