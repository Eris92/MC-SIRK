# SIRK Management Platform 0.1.1-dev.106

Date: 2026-08-13
Status: development pre-1.0, real SMTP Relay acceptance pending

## Scope

Restore system integration environment variables for PowerShell workflows whose credential assignments use stable workflow keys.

## Correction

- Read system credential assignments from `@workflow:<name>` before checking the legacy normalized script path.
- Pass the complete decorated script to system-environment resolution so its `SirkWorkflow` header is available.
- Preserve compatibility with existing path-key assignments.

## Root cause

The credentials editor stored SMTP Relay under `@workflow:relaymailsend`, while the PowerShell executor only checked `mail/send relay mail.ps1`. The script therefore received an empty `MYSCRIPTS_SMTP_SERVER` despite a configured and selected SMTP Relay profile.

## Verification

- SMTP Relay regression uses only the stable workflow assignment and verifies every `MYSCRIPTS_SMTP_*` environment value.
- JavaScript syntax, repository metadata and architecture validators pass.

## Version

`0.1.1-dev.105 -> 0.1.1-dev.106`.

No tag or GitHub Release is created.
