# SIRK Management Platform 0.1.1-dev.100

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral acceptance pending

## Scope

Make the cache administration script names neutral so their visible identity does not imply that cached data can only be supplied by Jira.

## Correction

- Rename `Jira Cache Assets.ps1` to `Cache Assets.ps1`.
- Rename `Jira Cache Users.ps1` to `Cache Users.ps1`.
- Change the visible labels to `Cache - sprzęt` and `Cache - użytkownicy`.
- Preserve `JiraAssetsCache` and `JiraUsersCache` workflow identities so credentials, execution routing and existing consumers continue to work.
- Update the hourly Jira scheduler policy path to the renamed Assets script.
- Keep the existing JSON cache paths and formats unchanged.

## Verification

- Jira asset cache, protocol wizard and hourly scheduler regressions pass.
- Repository and architecture validators pass.
- Real MeshCentral smoke remains pending.

## Version

`0.1.1-dev.99 -> 0.1.1-dev.100`.

No tag or GitHub Release is created.
