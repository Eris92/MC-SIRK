# MC-SIRK 0.1.1-dev.24

Status: development pre-1.0. No tag or GitHub Release.

## Runtime included

- Continues Issue #121 after real `0.1.1-dev.23` MeshCentral smoke showed SIRK left-menu icons briefly rendering with the configured colored/custom SVG source and then being replaced by white/currentColor Font Awesome icons.
- Root cause: `core.ensureMenu()` already selected the icon family/source through `SirkIconMode`, but deferred `public/shared/ui/page.js` installed a second Modern icon map and replaced the first icon after initial paint.
- `public/shared/ui/page.js` now keeps native left-menu class and active-state normalization without choosing or replacing Modern icon family/source.
- Classic/native `.lbtg` geometry normalization remains and preserves the source selected by `core.ensureMenu()`.
- Existing `Auto` / `Classic` / `Modern` setting remains the single icon policy; no additional filter toggle or CSS color filter was added.
- No polling, MutationObserver, timer, additional menu renderer or new icon state owner was introduced.

## Verification

- Regression: `test/modern-left-menu-native-icon.test.js` now requires the SirkIconMode-owned custom SVG source and `data-sirk-icon-family` marker to survive deferred `page.js` load.
- Existing Classic contract: `test/native-left-menu-contract.test.js` remains required.
- Existing admin/runtime icon-mode flow and security tests remain part of full `npm test`.
- PR #217 pre-bump full `npm test`: GREEN on run `31268474741`.
- Final PR CI on `0.1.1-dev.24` must remain GREEN.
- Real MeshCentral smoke remains required before closing #121: F5 plus Admin `Auto -> Classic -> Modern`, verifying no purple/custom -> white icon replacement and correct persistence after reload/navigation.

## Versioning

- Development revision only: `0.1.1-dev.23` -> `0.1.1-dev.24`.
- Version remains below `1.0.0`.
- No tag or GitHub Release.
