# SIRK Management Platform 1.7.8

- Places the Quick Commands output visibility button before Search.
- Keeps Search as the final left-side toolbar action.
- Sorts standard and custom toolbar actions together by their configured order.
- Keeps the Quick Commands toolbar on one line at compact panel widths.
- Makes the opened search field shrink inside the available toolbar space instead of wrapping below the buttons.
- Adds a regression test for button order and single-line search layout.
