# SIRK Management Platform 1.6.79

- Select the active SIRK left-menu entry with MeshCentral's full `lbbuttonsel2` state immediately when opening it.
- Resolve the active module from the live SIRK workspace instead of relying only on the `viewmode` URL parameter.
- Keep `My Devices` deselected when MeshCentral rewrites the current URL or redraws its native navigation.
