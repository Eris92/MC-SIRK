# SIRK Management Platform 1.6.79

- Resolve the active SIRK module from the rendered workspace state instead of relying on a stale `viewmode` query parameter.
- Give the active legacy left-menu item a deterministic light filled background matching native MeshCentral selection.
- Keep `lbbuttonsel2`, `aria-current=page`, and a plugin-owned selected class synchronized after native redraws.
- Observe both class and inline-style updates made by MeshCentral.
- Preserve fallback support for modern navigation layouts.
