# SIRK Management Platform 1.7.18

- Replaces the copied workspace-title CSS with a real native MeshCentral `<h1>` title structure.
- Detaches and restores the original My Devices title DOM without destroying nodes or event handlers.
- Removes Approval Center-owned toolbar and column geometry.
- Removes the Approval-only collapsed-layout exception from shared styles.
- Makes Approval Center and My Scripts inherit the same SharedToolbar and SharedLayout rules, including future theme and shared-style changes.
- Replaces the previous visual-copy test with an inheritance and native-DOM regression contract.
