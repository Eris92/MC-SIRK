# SIRK Management Platform 1.7.20

- Gives the red Quick output-attention state visual precedence over the normal active-button state.
- Suppresses the transient blue `is-active` paint whenever the output panel is canonically hidden.
- Keeps the button neutral between remount and the red attention class being synchronized.
- Preserves the normal active style after the user opens the output and clears attention.
- Adds a regression test for hidden-output attention precedence during tab changes and script rerenders.
