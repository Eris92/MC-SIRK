# SIRK Management Platform 1.7.20

- Gives the red Quick output-attention state visual precedence over the normal active-button state.
- Suppresses the transient blue `is-active` paint whenever the output panel is canonically hidden.
- Clears output attention when the user changes a native MeshCentral tab because the Quick output is discarded by that navigation.
- Marks the current output as already acknowledged and clears pending state, preventing the same text from turning the button red again after returning.
- Preserves the normal active style after the user opens the output and allows only a genuinely new result to raise attention again.
- Adds regression coverage for hidden-output attention precedence and native-tab attention reset.
