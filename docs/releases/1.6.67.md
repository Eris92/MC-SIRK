# SIRK Management Platform 1.6.67

- Hook script selection at the shared directory-tree layer before the Commands page is mounted.
- Make the native Commands Run action explicitly visible in MeshCentral.
- Automatically invoke Run for variable-free commands and scripts after their definition is rendered.
- Keep scripts with runtime variables manual and prevent duplicate execution.
