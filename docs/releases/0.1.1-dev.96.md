# SIRK Management Platform 0.1.1-dev.96

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral acceptance pending

## Scope

Align the seeded My Scripts layout with the operator's local organization of Jira cache administration scripts.

## Correction

- Move `Jira Cache Users.ps1` to `settings/Jira`.
- Move `Jira Cache Assets.ps1` to `settings/Jira`.
- Keep `Jira Asset Protocol.ps1` in the top-level `Jira` directory.
- Keep `jira-users-cache.json` and `jira-assets-cache.json` in their existing runtime data location.
- Update Jira workflow regressions to use the new script paths.

## Verification

- Jira asset option and wizard regressions pass with `settings/Jira` paths.
- Repository layout and metadata validation required before integration.
- `git diff --check` passes.

## Version

`0.1.1-dev.95 -> 0.1.1-dev.96`.

No tag or GitHub Release is created.
