# SIRK Management Platform 1.7.6

- Exposes `quick-output-state.js` through `pluginadmin.ashx` with `text/javascript; charset=utf-8`.
- Fixes the browser MIME refusal that stopped the entire SIRK startup chain and removed the left menu, My Scripts, Approval Center and Commands.
- Adds a startup asset contract test so every JavaScript file referenced by `plugin-main.js` must also be mapped by the plugin asset router and exist on disk.
- Retains the Quick output visibility and unseen-result attention behavior introduced for 1.7.5.
