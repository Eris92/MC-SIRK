# SIRK Management Platform 1.6.81

- Track the lifetime of an active SIRK workspace on the document root.
- Preserve the explicit filled selection introduced in 1.6.80.
- Visually suppress a stale `My Devices` selection if MeshCentral re-applies it during a native `go(1)` redraw.
- Let MeshCentral provide the selected item background so the highlight follows the current light or night theme.
