# 0.1.1-dev.44

Status: **development pre-1.0**. Shared execution parameter dialog plus real Windows shell smoke infrastructure. No tag or GitHub Release.

## Scope

### #253 Shared native execution parameter dialog
- add one native MeshCentral parameter-input lifecycle exposed through `SharedScriptTools`;
- use Modern `setModalContent` / `showModal` and Classic `setDialogMode`, without a plugin-owned overlay;
- support text, select, switch, user and asset controls with shared required validation;
- move Quick, My Commands and My Scripts parameter input out of inline details while preserving their existing Output/Results owners;
- preserve no-variable auto-execution, confirmation, approval semantics and `variableValues` payload shape;
- collect Multi parameters once before target selection;
- keep independent deferred assets parallel while serializing only the real `script-tools -> parameter-dialog -> Quick` dependency;
- add one bounded dynamic-option provider hook with stale-response suppression and asset refresh after a real user change;
- add no polling, `MutationObserver`, timer-driven readiness or browser `prompt` input.

### #238 Windows interactive Shell smoke
- keep the already integrated real `windows-latest` Windows PowerShell 5.1 smoke in the maintained `test.yml`;
- validate read-only `Shell.Application.NameSpace(49)` resolution and reject the historical `shell:ConnectionsFolder` input;
- do not invoke `Properties` or mutate network configuration.

## Verification
- #253 pre-bump full Linux `npm test`: GREEN, Actions run `31394561056` / Test #607;
- #253 pre-bump Windows interactive Shell smoke: GREEN, same run;
- #238 original Linux + Windows evidence: GREEN, Actions run `31390869438`;
- final exact-version `0.1.1-dev.44` Linux + Windows CI is required before merge.

## Required real smoke
Keep Issue #253 open until evening MeshCentral smoke confirms parameterized execution in Quick, My Commands and My Scripts in Modern/Classic and light/dark, including cancel, required validation, no-vars auto-run and one-submit behavior. Issue #238 is already complete and does not require network mutation.
