# 0.1.1-dev.44

Status: **development pre-1.0**. Windows interactive Shell smoke infrastructure for Issue #238. No tag or GitHub Release.

## Scope

### #238 Windows interactive Shell smoke
- keep Linux `npm test` as the canonical broad suite;
- add a bounded `windows-latest` job to the existing maintained `test.yml` workflow;
- run the targeted harness with Windows PowerShell 5.1;
- validate real `Shell.Application.NameSpace(49)` Network Connections namespace resolution;
- reject the historical dev.33 `shell:ConnectionsFolder` input before COM invocation;
- do not invoke `Properties` or any other Shell verb and do not mutate network configuration;
- add no polling or UI readiness loop.

## Verification
- pre-bump Linux `npm test` job: GREEN, Actions run `31390869438`;
- pre-bump Windows interactive Shell smoke: GREEN, same run;
- final exact-version `0.1.1-dev.44` Linux + Windows jobs are required before merge.

## Follow-up
The harness closes the infrastructure gap for safe namespace-level Windows evidence. Issue #128 remains a separate runtime problem and must use this harness only as evidence support; the smoke must not be expanded into network mutation.
