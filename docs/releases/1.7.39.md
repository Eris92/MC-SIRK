# SIRK Management Platform 1.7.39

- Restores the visible active highlight for SIRK entries in the modern MeshCentral left sidebar.
- Applies both the Bootstrap `active` state and MeshCentral's native `lbbuttonsel2` selected state to modern `a.nav-link` entries.
- Keeps the classic MeshCentral left menu on its existing compact `lbbuttonsel` contract.
- Covers My Scripts, Commands, Approval Center and Move Requests with one shared regression test.
- Clears all selected classes and `aria-current` when a module becomes inactive.
