# SIRK Management Platform 0.1.1-dev.89

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral acceptance pending

## Scope

Move the `Wymuś odświeżenie` checkbox to the left of its label in both Jira cache dialogs.

## Correction

- Recognize the existing `JiraAssetsCache` and `JiraUsersCache` workflow markers in the Jira dialog decorator.
- Prepare a copied cache-dialog definition with `inlineControl` enabled for the `Force` switch.
- Render the checkbox first and its label second using the existing shared inline-switch contract.
- Do not mutate canonical script definitions or change non-cache parameter dialogs.

## Verification

- Jira wizard regression verifies one native cache dialog, left-side inline switch and immutable source definition.
- Jira cache contract and shared parameter-dialog regression pass.
- JavaScript syntax and `git diff --check` pass.
- Full `npm test` required before integration.

## Version

`0.1.1-dev.88 -> 0.1.1-dev.89`.

No tag or GitHub Release is created.
