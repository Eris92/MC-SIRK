# SIRK Management Platform 1.7.7

- Rebuilds Commands routing around MeshCentral's native view 19 and nested plugin-page lifecycle.
- Selects Commands only when both the native Plugins view and the Commands nested page are active, preventing multiple selected host tabs.
- Restores the previously active native plugin page whenever the user leaves Commands or opens Plugins.
- Reconciles routing after MeshCentral clears and recreates `p19headers` and `p19pages` during device refresh.
- Remounts Commands into the replacement page host after F5, without repeatedly remounting the same host.
- Preserves the native `Plugins - <device>` title outside Commands and shows `Commands - <device>` only while Commands is active.
- Adds a functional lifecycle test covering stale stored state, single-selection invariants, F5 host replacement and same-view Plugins navigation.
