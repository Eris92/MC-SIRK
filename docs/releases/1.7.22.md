# SIRK Management Platform 1.7.22

- Removes the Bootstrap pointer-focus frame from Quick toolbar buttons after mouse clicks.
- Uses `:focus:not(:focus-visible)` with important precedence so the frame does not reappear after Quick rerenders.
- Preserves the visible keyboard focus ring for `Tab` navigation.
- Keeps active, favorites and red output-attention states unchanged.
- Extends the Quick visual contract test with pointer and keyboard focus assertions.
