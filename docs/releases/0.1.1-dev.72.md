# SIRK Management Platform 0.1.1-dev.72

Status: development pre-1.0.

## Changes

- Preserve explicit Jira `user` and `referencedObject` identities in the compact Assets cache regardless of localized or non-semantic attribute names.
- Keep plain-string user matching restricted to assignment-semantic attributes, preventing unrelated identity text from becoming an assignment match.
- Invalidate schema v3 Assets snapshots with schema v4 so the protocol rebuilds snapshots that could lose authoritative Jira reference identities.
- Carry forward the bounded concurrent Assets refresh from the previous runtime correction.
- Make every subsequent technical build intended for user testing increment the `0.1.1-dev.X` revision so the installed runtime is unambiguous.

## Verification

PR #319 candidate Actions run `31603013952`: full `npm test` GREEN and Windows interactive-shell smoke GREEN before the version bump.

The version bump itself must pass repository metadata validation and the full test workflow before merge.

No tag or GitHub Release. Product remains below `1.0.0`.
