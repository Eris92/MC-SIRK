# SIRK Management Platform 0.1.1-dev.60

## Equipment modal width

The Jira equipment step opts into the shared parameter dialog's intrinsic option-width contract. Its native Modern modal expands to the longest equipment label and keeps each row on one line. Width remains bounded by the viewport; when a label cannot fit even at that limit, the existing checklist overflow provides horizontal scrolling instead of wrapping the asset description.
