# SIRK Management Platform 0.1.138

**Status:** development pre-1.0  
**Issue:** #418

## Zmiany

- Dodano `seed/MyScripts/Entra ID/Get-SmsVoicePolicyUsers.ps1` jako natywny My Scripts report.
- Skrypt nie używa `Connect-MgGraph` ani interaktywnego logowania; pobiera token Microsoft Graph przez app-only client credentials z istniejącej globalnej integracji Entra/AAD.
- Wykorzystuje istniejący kontrakt `SirkSystemCredential: Entra` i `MYSCRIPTS_ENTRA_*`; nie wymaga duplikowania sekretów per skrypt.
- Odczytuje registration campaign oraz konfiguracje metod `sms` i `voice`, rozwiązuje nazwy grup i generuje UTF-8 CSV z zakresem Include/Exclude.
- Emituje istniejący kontrakt Results: `Wygenerowano raport CSV`, `Liczba rekordow` i `CSV_DOWNLOAD:`, dzięki czemu `Download CSV` korzysta z obecnego chronionego endpointu bez nowego renderera lub route.

## Weryfikacja

- targeted regression sprawdza odkrywalność My Scripts, binding do Entra, brak zależności od Graph PowerShell oraz kontrakt CSV download;
- pełne `npm test` wymagane przed merge;
- brak taga i GitHub Release.
