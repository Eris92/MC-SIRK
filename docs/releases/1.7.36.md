# SIRK Management Platform 1.7.36

- Stops creating a plugin-owned `<h1>` for My Scripts and Approval Center.
- Reuses the exact native MeshCentral `p1title` heading element, including its classes, theme styles and attached controls.
- Replaces only the existing native title text node when switching between plugin workspaces.
- Restores the exact original native title text when leaving the workspace.
- Detects and adopts a replacement `p1title` when MeshCentral rebuilds the page during refresh.
- Restores the old title before adopting the refreshed native heading.
- Keeps My Scripts and Approval Center automatically synchronized with every native or custom MeshCentral heading style change.
- Adds functional DOM regression coverage for node identity, class preservation, module switching, refresh replacement and restoration.
