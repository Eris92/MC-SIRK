# MC-SIRK 0.1.1-dev.21

Status: development pre-1.0. No tag or GitHub Release.

## Runtime included

- PR #209 / commit `b4be0ab28d788bc3596e63057a9d6667b79a16f3`: Move Request host action no longer depends on the historical `0/100/400/1000/2000/4000 ms` readiness timer staircase.
- `onDeviceRefreshEnd` and `onNativePageEnd` now reuse the existing idempotent `installHostButton()` owner synchronously through `syncHostButton()`.
- No `MutationObserver`, polling loop, extra request or second DOM owner was added.
- Existing `hostButtonEnabled`, single connected `#MoveRequestHostButton`, node resolution, native cloned presentation and dialog flow are preserved.

## Verification

- Targeted regression: `test/move-request-host-button-readiness.test.js`.
- Canonical PR #209 `npm test`: GREEN, run `31259892140`.
- Real MeshCentral hard-refresh/navigation smoke remains required for Issue #178 before that Issue can be closed.

## Versioning

- Development revision only: `0.1.1-dev.20` -> `0.1.1-dev.21`.
- Version remains below `1.0.0`.
- No tag or GitHub Release.
