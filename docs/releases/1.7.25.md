# SIRK Management Platform 1.7.25

- Makes multi-device execution available by default for My Scripts files without a `MultiHost` directive.
- Keeps `# MultiHost: false` as an explicit per-script opt-out.
- Applies the same availability rule to the script tree, row actions, single-script details and backend execution.
- Leaves My Commands parsing and behavior unchanged.
- Adds regression coverage for default, explicit enable and explicit disable cases.
