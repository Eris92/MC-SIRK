# SIRK Management Platform 1.7.38

- Aligns the second-column origin in Approval Center, Commands and My Scripts.
- Replaces the content-dependent first track with one exact shared width: 220 px on desktop, 190 px below 1000 px and 64 px when collapsed.
- Applies one zero-inset layout-host contract to all three modules.
- Keeps Edit and Multi measured geometry compatible with the shared baseline width.
- Uses one synchronized Collapse state for Approval Center, Commands and My Scripts.
- Migrates the first available legacy per-module Collapse setting to the shared key.
- Synchronizes already mounted module layouts immediately when Collapse changes.
- Preserves the stacked single-column layout below 800 px.
- Adds functional regression coverage for all three modules and their shared Collapse lifecycle.
