# SIRK Management Platform 1.7.32

- Replaces the previous `max-content` Edit geometry with the working layout contract from the supplied `preview.html` reference.
- Keeps the first column unchanged in normal, collapsed, Edit and Multi states.
- Expands only the second column by adding a separate fixed-width four-button action area.
- Keeps long script labels wrapped inside a stable `clamp(280px, 32vw, 430px)` text area.
- Uses the same Edit layout in My Scripts and Commands.
- Makes Edit and Multi a real toolbar switch in Commands: the active mode remains visible and replaces the other mode button; closing the mode restores both choices.
- Places Edit and Multi directly beside each other before Refresh so they occupy the same toolbar slot.
- Keeps My Scripts single-device-only.
- Adds regression coverage for the preview-derived layout and toolbar switch behavior.
