# SIRK Management Platform 1.6.75

- Accept canonical MeshCentral node identifiers from the default domain in the form `node//<id>`.
- Keep support for named-domain identifiers in the form `node/<domain>/<id>`.
- Fix Commands falsely reporting that the current device identifier could not be determined on default-domain installations.
- Add regression coverage for both node identifier forms.
