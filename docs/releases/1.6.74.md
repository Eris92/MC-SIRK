# SIRK Management Platform 1.6.74

- Preserve the native MeshCentral device identifier immediately when `onDeviceRefreshEnd` fires, even when the SIRK browser runtime has not loaded yet.
- Replay the captured device context after browser runtime initialization so Commands receives the correct `node/<domain>/<id>` value.
- Prevent the startup timing race that produced `Unable to determine the current device identifier` on a valid device page.
- Add regression coverage for early device-context capture and replay.
