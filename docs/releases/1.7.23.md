# SIRK Management Platform 1.7.23

- Removes the shared toolbar `is-active` outline from the Quick output action.
- Keeps `Pokaż wynik / Show output` visually neutral during category and script rerenders.
- Applies the neutral output-button appearance whether the result pane is hidden or open.
- Preserves the red `has-output-attention` state for genuinely new hidden results.
- Preserves the keyboard `focus-visible` ring while suppressing only the incorrect active outline.
- Adds regression coverage for the shared toolbar outline and Quick-specific override.
