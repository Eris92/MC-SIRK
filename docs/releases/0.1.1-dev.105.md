# SIRK Management Platform 0.1.1-dev.105

Date: 2026-08-13
Status: development pre-1.0, real SMTP Relay acceptance pending

## Scope

Remove the requirement to provide an SMTP sender while keeping every generated message valid for `System.Net.Mail`.

## Corrections

- Consider SMTP Relay configured when server and port are present; `Default sender` is optional.
- Resolve the effective sender in this order: script `Sender`, integration `Default sender`, `sirk@localhost`.
- Use port `25` when the SMTP port environment value is empty.
- Report a dedicated configuration error when the SMTP server itself is missing.

## Verification

- SMTP Relay workflow regression verifies readiness without a default sender and both script fallbacks.
- PowerShell syntax, repository metadata and architecture validators pass.

## Version

`0.1.1-dev.104 -> 0.1.1-dev.105`.

No tag or GitHub Release is created.
