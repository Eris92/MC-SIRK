# SIRK Management Platform 1.7.35

- Applies the same live measured action geometry to Multi-device mode as Edit mode.
- Keeps the first column pixel-identical when Multi is enabled.
- Keeps the script text width and wrapping identical to the normal view.
- Expands only the second column by the real rendered width of the Multi action button plus the safety gutter.
- Uses the same action row, border-box sizing and mobile fallback for Edit and Multi.
- Removes the old percentage-based Multi layout.
- Keeps Edit and Multi mutually exclusive: activating one disables and hides the other until the active mode is closed.
- Keeps My Scripts single-device-only.
- Adds regression coverage for four Edit buttons and one Multi button.
