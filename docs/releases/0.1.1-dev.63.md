# SIRK Management Platform 0.1.1-dev.63

## Concise Jira cache dialogs

Both explicit cache scripts now open without script-level explanatory text or checkbox help copy. The dialog body contains only the `Wymuś odświeżenie` checkbox, followed by the native actions.
