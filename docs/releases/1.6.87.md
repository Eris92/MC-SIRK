# SIRK Management Platform 1.6.87

- Use the existing global `MeshPluginCore.activePlugin` as the single owner shared with standalone MeshCentral plugins.
- Keep `SirkPlatformCore` functions isolated without replacing another plugin's `MeshPluginCore` implementation.
- Allow standalone plugins to close an active SIRK module and allow SIRK to close the previously active standalone plugin.
- Prevent simultaneous active-menu state caused by two independent plugin-owner namespaces.
- Add a regression test covering both load orders and bidirectional active-owner visibility.
