# 1.8.15

- Fix the root cause of inconsistent Approval Center, Commands, My Scripts and Quick list styling.
- Apply the final visual contract by the SIRK ownership marker instead of requiring Bootstrap `list-group-item` or `nav-link` classes.
- Cover both Quick columns and every shared first/second-column row regardless of MeshCentral UI variant.
- Replace stale shared-list observers and theme-adapter wrappers after an update.
- Expose the active contract version as `data-sirk-list-contract-version="1.8.15"` on the document root and style element.
- Keep the Requires Approval indicator in the first text row.
- Add regression coverage that rejects class-dependent selectors.
