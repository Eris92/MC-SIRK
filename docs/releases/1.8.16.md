# 1.8.16

- Remove the extra `mc-catalog-navigation` and `mc-catalog-roots` layout layers from Commands and My Scripts.
- Render Results and catalog root rows directly inside the real `mc-shared-primary` column, matching Quick's DOM ownership.
- Preserve Results-first and Results-last ordering through a comment anchor during recursive tree renders.
- Replace only generated catalog roots during selection changes instead of rebuilding an intermediate list-group container.
- Expose the active catalog structure as `data-sirk-catalog-contract-version="1.8.16"` on the primary column.
- Add a functional regression test for initial rendering, root rerendering and both Results positions.
