# SIRK Management Platform 1.7.28

- Keeps the normal Commands column widths when Edit mode is enabled.
- Makes Edit mode add only the existing row action buttons.
- Prevents Edit from expanding the script text area or changing the grid geometry.
- Keeps the wider second column exclusively for Commands multi-device mode.
- Preserves the current label wrapping and action-button behavior.
- Updates the layout regression test for the Edit-only-actions contract.
