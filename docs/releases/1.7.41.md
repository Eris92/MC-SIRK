# SIRK Management Platform 1.7.41

- Gives every `.mc-shared-page` one continuous theme-aware workspace surface.
- Removes the accidental visual difference where Commands inherited the gray device-page background while My Scripts and Approval Center inherited the black `p1` background.
- Uses Bootstrap body variables when available and supports classic MeshCentral `night` and `dark` modes.
- Keeps toolbar, tabs and the three-column layout on the same inherited surface.
- Does not change Approval cards, selection colors, column widths, Edit/Multi behavior or menu icons.
- Adds regression coverage preventing module-specific black workspace backgrounds.
