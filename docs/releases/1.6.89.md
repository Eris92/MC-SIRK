# SIRK Management Platform 1.6.89

- Introduce one shared `.sirk-button` action-button contract based on MeshCentral's secondary button appearance.
- Apply the same dimensions, typography, border, hover, active, focus and disabled states to toolbar, Run, result, dialog and Settings action buttons.
- Keep navigation rows, folder rows, tabs and status selectors on their existing layout-specific styles.
- Load the shared contract after the Settings compatibility stylesheet so legacy blue button overrides no longer win.
