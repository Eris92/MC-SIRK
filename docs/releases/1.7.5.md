# SIRK Management Platform 1.7.5

- Replaces the conflicting Quick output preference bridge with a dedicated output-state controller.
- Keeps a hidden output pane hidden throughout command start, execution and completion.
- Prevents the third column from reopening after a command finishes.
- Marks the output button with a subtle red attention state when a completed hidden result arrives.
- Clears the attention state after the user opens the output pane.
- Calls the native output toggle only when the internal pane state actually needs changing.
- Does not modify Commands/Plugins routing or any global MeshCentral navigation state.
