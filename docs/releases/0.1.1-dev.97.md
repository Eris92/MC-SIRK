# SIRK Management Platform 0.1.1-dev.97

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral acceptance pending

## Scope

Provide an internal Windows Task Scheduler installer that force-refreshes both Jira cache files every hour.

## Correction

- Add `seed/MyScripts/_Scheduler/Install Jira Cache Scheduler.bat`.
- Register `MC-SIRK Jira Cache Refresh` as a `SYSTEM` task with a one-hour repetition interval.
- Start the first refresh immediately after task registration.
- Add a Node runner that reuses the canonical settings, encrypted secret store and Jira cache service.
- Read the Assets AQL and label attribute from `settings/Jira/Jira Cache Assets.ps1`.
- Keep JSON files in the existing `sirk-platform-data` root.
- Treat stale fallback as a failed scheduled refresh so Task Scheduler exposes a non-zero result.
- Keep `_Scheduler` internal and absent from the public My Scripts tree.

## Verification

- Scheduler regression performs a real mocked users and Assets refresh and verifies both JSON outputs.
- BAT contract requires hourly execution, `SYSTEM`, immediate first start and no embedded token.
- BAT help path runs without changing Task Scheduler.
- Windows trigger construction reports `PT1H`.
- Repository layout and development metadata validation pass.

## Version

`0.1.1-dev.96 -> 0.1.1-dev.97`.

No tag or GitHub Release is created.
