# SIRK Management Platform 0.1.1-dev.101

Date: 2026-08-13
Status: development pre-1.0, real SMTP Relay acceptance pending

## Scope

Add an internal no-authentication SMTP Relay integration and an approval-protected mail workflow.

## Changes

- Add SMTP host, port, default sender, TLS, attachment root and attachment-size settings.
- Add the `SMTP Relay` system credential profile without storing an unnecessary password.
- Add `Mail/Send Relay Mail.ps1` with To, CC, BCC, subject, text/HTML body and attachments.
- Add reusable multiline parameter metadata and textarea rendering for message body and attachment paths.
- Restrict resolved attachment paths to the configured server root and bound their total size.
- Keep addresses, body and attachment paths out of script output.

## Verification

- SMTP integration normalization, readiness and executor environment regression passes.
- Script metadata, multiline fields and attachment-boundary contracts pass.
- PowerShell AST parsing, integration admin/health and system credential regressions pass.
- Real relay delivery remains pending because no live SMTP message was sent.

## Version

`0.1.1-dev.100 -> 0.1.1-dev.101`.

No tag or GitHub Release is created.
