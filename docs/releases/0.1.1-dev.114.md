# 0.1.1-dev.114

Status: development pre-1.0  
Date: 2026-08-17

## Jira Asset Protocol #355 header layout follow-up

Real generated-PDF feedback on `0.1.1-dev.113` showed that the logo and protocol title were vertically centered too close to one another in the shared A4 header.

Changes:

- keep the existing shared A4 document template as the single header owner;
- top-align the existing logo/fallback block inside the header;
- move the existing document title 12 px lower relative to the logo using the current `.title` rule;
- add no new CSS class, renderer, layout owner, timer, observer or request;
- preserve Jira protocol content, confirmation, protected PDF and CMDB semantics unchanged.

Verification:

- targeted Jira protocol runtime test checks the header geometry contract;
- full `npm test` and Windows interactive-shell smoke must be green on the exact dev.114 PR head before merge.

No tag or GitHub Release is created for this development revision.
