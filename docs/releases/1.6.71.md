# SIRK Management Platform 1.6.71

- Run logged-on-user scripts through `wscript.exe` instead of starting PowerShell directly from Task Scheduler.
- Keep the helper process completely hidden while preserving the real interactive user token and profile.
- Continue returning script output and exit status to MeshCentral.
