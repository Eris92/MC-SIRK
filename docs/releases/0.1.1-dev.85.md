# SIRK Management Platform 0.1.1-dev.85

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral/Jira acceptance pending

## Scope

Make the shared protocol template neutral when no custom logo is configured.

## Correction

- Replace the hard-coded `INVESTA` fallback label with `LOGO`.
- Change the embedded custom-logo alt text to neutral `Logo`.
- Preserve custom/uploaded logo embedding, layout, Edge LocalSystem rendering, retry and final PDF fallback unchanged.

## Verification

- Targeted renderer regression requires the neutral `LOGO` placeholder and rejects `INVESTA` when the configured logo file is absent.
- Full `npm test` required before integration.

## Version

`0.1.1-dev.84 -> 0.1.1-dev.85`.

No tag or GitHub Release is created.
