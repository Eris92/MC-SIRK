# SIRK Management Platform 1.6.72

- Execute a variable-free command or script directly from the native Commands selection callback.
- Keep variable-based scripts manual and show a visible Run action.
- Disable the obsolete DOM-click auto-run workaround when the native Commands module is active.
- Preserve selected-item rendering without re-running a script after page refresh or remount.
