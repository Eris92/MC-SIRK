# SIRK Management Platform 1.7.40

- Replaces custom `<img>` icons in the modern MeshCentral left sidebar with native Font Awesome source markup.
- Uses the same `fa-solid ... me-2` structure as built-in MeshCentral entries so Font Awesome converts the icon to inline `<svg>`.
- Removes custom `width`, `height`, `object-fit` and image-specific spacing from SIRK menu icons.
- Keeps the exact inactive anchor classes copied from `LeftMenuMyDevices`.
- Keeps MeshCentral's native `active lbbuttonsel2` selected state in the modern UI and `lbbuttonsel` in the classic UI.
- Covers Approval Center, My Scripts, Commands and Move Requests.
- Adds regression coverage for native anchor classes, Font Awesome structure, spacing and active state.
