# SIRK Management Platform 1.6.66

- Selecting a variable-free script in the native Commands tree now invokes its existing Run action reliably.
- The fallback also works for Commands views mounted before the compatibility wrapper is installed.
- File-backed scripts are awaited until their definition is loaded; scripts with runtime variables remain manual.
- Stale selections and duplicate automatic runs are ignored.
