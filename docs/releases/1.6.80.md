# SIRK Management Platform 1.6.80

- Render the active SIRK legacy left-menu item with a deterministic light filled background.
- Resolve the active module from `SirkPlatformCore.workspaceState.viewMode` before native view or URL fallbacks.
- Keep `lbbuttonsel2`, `aria-current=page`, and the plugin-owned selected class synchronized.
- Reapply selection after MeshCentral changes menu classes or inline styles.
- Use idempotent inline-style updates to avoid mutation-observer loops.
