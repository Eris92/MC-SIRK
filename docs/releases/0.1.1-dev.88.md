# SIRK Management Platform 0.1.1-dev.88

Date: 2026-08-13
Status: development pre-1.0, real MeshCentral/Jira acceptance pending

## Scope

Make the Jira Asset Protocol result concise and operational: show only selected equipment as a table, keep all actions together and retain the printable protocol text outside the primary Output.

## Correction

- Return the primary protocol `output` as a `meshTable` JSON payload containing equipment rows only.
- Use the normalized fields Hostname, Producent, Model, Numer seryjny, Numer inwentarzowy and Asset ID.
- Keep the complete printable protocol in `rawOutput` for `Debug / raw output` and PDF fallback diagnostics.
- Show `PROTOKÓŁ PRZEKAZANIA SPRZĘTU` or `PROTOKÓŁ ZWROTU SPRZĘTU` above the result controls.
- Reuse the shared action row so `Copy`, `Open PDF` and `Download PDF` remain adjacent.
- Preserve the existing protected artifact URLs and PDF generation contract.

## Verification

- Real service test verifies the equipment-only JSON schema and absence of user metadata in table rows.
- Renderer regression verifies the pre-paint workflow heading and workflow-specific raw diagnostic value.
- Native Modern/Classic result-dialog contract remains covered.
- JavaScript syntax checks and `git diff --check` pass.
- Full `npm test` required before integration.

## Version

`0.1.1-dev.87 -> 0.1.1-dev.88`.

No tag or GitHub Release is created.
