# 0.1.131

## Scope

- Record the real `0.1.130` smoke: CSV download is fixed, but duplicate report executions still reach the backend despite browser-side single-flight.
- Add server-side single-flight in the existing `server/core/server-script-executor.js` owner for ordinary My Scripts executions.
- The active key is a SHA-256 over requester identity, script path/hash and validated effective variable values; secrets are never stored in the key material beyond the in-process hash operation.
- Identical active executions reuse one Promise and therefore one child process; different requester/script/effective values remain independent.
- Release active state on both success and failure; keep the existing global executor queue and all special executionOptions paths unchanged.
- No timer, observer, polling loop or new service owner.

## Verification

- `test/server-script-singleflight.test.js` covers duplicate active execution, success/failure release, different requester and different effective values.
- Full `npm test` required because the shared server-side My Scripts executor contract changes.

Issue #399 remains open until real MeshCentral smoke confirms one backend report execution per user action.
