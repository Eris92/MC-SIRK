# SIRK Management Platform 0.1.1-dev.75

Date: 2026-08-12
Status: development pre-1.0, real MeshCentral/Jira acceptance pending

## Scope

Restore the last real-smoke-working Jira equipment source contract from `0.1.1-dev.64` after repeated workspace-wide matching corrections in dev.65-dev.74 still left `Sprzęt do protokołu` empty.

## Changes

- Restore canonical `Jira Asset Protocol` AQL to `objectType in objectTypeAndChildren("Sprzęt użytkownika")` exactly as in dev.64.
- Restore `Jira Cache Assets` to the same dev.64 AQL.
- Remove the protocol-only `SirkJiraAssetMaxResults: 5000` override and return to the existing default bounded selector limit used by dev.64.
- Keep the current Jira service improvements: users-cache v2, compact Assets cache v4, bounded concurrent refresh, response attribute metadata, reference handling and PDF fixes.
- Keep generic script-owned Jira Assets support unchanged; only the canonical protocol/cache seed scope is rolled back.

## Reason

Real dev.64 evidence showed the selector returned assigned computer-class equipment. The empty-list regression appeared after dev.65 changed the canonical source from the equipment hierarchy to workspace-wide `Key is not EMPTY`. This build stops speculative matcher changes and restores that known-working source contract while retaining later independent performance/cache fixes.

## Version

`0.1.1-dev.74 -> 0.1.1-dev.75`.

No tag or GitHub Release is created.
