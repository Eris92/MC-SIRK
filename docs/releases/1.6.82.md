# SIRK Management Platform 1.6.82

- Restore a SIRK workspace only when a real native `MainMenu` or `LeftMenu` control is activated.
- Keep clicks on blank space, gaps and the workspace header inside the current SIRK module.
- Give the active SIRK left-menu entry a theme-neutral blue fill and accent so it remains clearly selected.
