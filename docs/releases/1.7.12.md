# SIRK Management Platform 1.7.12

- Synchronizes the Quick output button title and `aria-label` with the canonical hidden-output state during the first render and every remount.
- Shows `Pokaż wynik` / `Show output` immediately when the result column is already hidden.
- Switches back to `Ukryj wynik` / `Hide output` immediately after the result column is opened.
- Adds a functional regression test for conflicting canonical and legacy Quick output state values.
