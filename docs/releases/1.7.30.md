# SIRK Management Platform 1.7.30

- Replaces the fixed Commands Edit column with the natural `max-content` layout used by the supplied MyScripts reference implementation.
- Sizes each Edit row from the script label plus the real four-button action group.
- Keeps desktop Edit labels on one line instead of wrapping them into a narrow strip.
- Reserves a 360 px minimum label area and a separate fixed 132 px action area.
- Keeps the details column on the remaining workspace width.
- Restores wrapped, stacked behavior on mobile widths.
- Preserves the Edit/Multi mutual-exclusion contract from 1.7.29.
- Adds regression coverage for the reference-derived natural-width layout.
