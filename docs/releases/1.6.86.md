# SIRK Management Platform 1.6.86

- Rebuild the shared SIRK menu lifecycle from the uploaded standalone MyScripts 1.9.7 implementation that worked correctly.
- Use one explicit `activePlugin` owner for Approval Center, Move Requests, My Commands and My Scripts.
- Enter native page `1`, close the previous plugin through `goPageStart`, mount the requested workspace, then clear and set native menu selection.
- Clear only selected entries inside `#MainMenuSpan` and `#page_leftbar`; unrelated plugin menu elements are no longer globally rewritten.
- Remove pointer interception, global menu CSS and class/style mutation monitoring from menu handling.
- Add a functional browser-DOM simulation covering Approval Center → My Scripts → Devices → third-party plugin switching.
